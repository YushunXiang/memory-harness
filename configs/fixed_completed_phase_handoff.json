{
  "name": "completed_phase_handoff",
  "deployable": true,
  "paths": [
    {
      "name": "completed_phase_handoff",
      "encoder": {"type": "tokens", "options": {"max_tokens": 1}},
      "writer": {"type": "always"},
      "store": {"type": "ring", "options": {"capacity": 500}},
      "retriever": {
        "type": "completed_phase_mean",
        "options": {"max_items": 1}
      },
      "lifecycle": {"type": "episode"}
    }
  ],
  "controller": {"type": "all"},
  "utilizer": {
    "type": "mem0_context",
    "options": {
      "embed_dim": 2048,
      "sliding_window_size": 30,
      "anchor_path": null,
      "history_path_quotas": {"completed_phase_handoff": 30}
    }
  }
}
