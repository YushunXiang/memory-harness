# E-MAC 本周计划（2026-08-14—2026-08-20）

本周目标：继续完成 Mem-0-on-π0.5 的固定 memory 复现，为后续 controller 和 Agent search 建立可用信号；项目状态为 **ACTIVE / reproduction in progress**，本周不训练 controller。Cover Blocks 用于检验 planner-side key；anchor/sliding 的主要对照转到 RMBench `M(1)` 任务。

补充直接复现线：用官方重训发布的 `Mem-0-m1mix-RMBench` checkpoint 在 Put Back Block
先跑 10 个固定 seeds 的 `full`，再对同一 checkpoint 做 `without_anchor / without_sliding`
推理 intervention。`full` 已复现 `10/10`；intervention 只检验执行时依赖，
不冒充论文 Table 2 的精确重放；论文正文与当前 release 均未说明或提供 anchor/sliding 消融的独立训练产物。

## 已完成

- 建立统一 architecture facade：`none / anchor / sliding / anchor+sliding / key / planner_no_key / consolidating` 均由配置组合。
- 移植 Mem-0 action-side 1+30 latent layout、phase reset 和共享 checkpoint 消融；4-seed oracle diagnostic 显示 anchor+sliding 的阶段 reward 高于 none，但 full success 均为 0。
- 接入 planner-side key history，并审计出旧 no-key 对照错误：论文 `w/o key` 应使用当前 observation 和独立 planner，而不是冻结初始图。
- 完成独立 no-key planner 的 75-step LoRA 训练；训练预算匹配的 key/no-key 在 30 条分层样本上分别达到 `30/30` 与 `26/30` exact match。
- 完成 4-seed key/no-key 忠实配对：key 平均 max/total reward 为 `0.075/91.175`，no-key 为 `0.0625/79.475`；key 为 2 胜、1 平、1 负，full success 均为 0。
- 完成 `consolidating vs sliding` 单 seed zero-shot screen：`0.15/175.05 vs 0.10/125.75`，并据此触发 matched finetune。
- 完成 consolidating 部署一致的 8677 段 context、同预算 50-update π0.5 fine-tune、checkpoint 审计和 4-seed matched-training diagnostic；consolidating 平均 max/total 为 `0.0875/107.925`，sliding 为 `0.1000/122.350`，1 胜 3 负且 full success 均为 0，因此不晋级 controller。
- 加入 MemoryVLA-style adjacent merge store；训练数据生成器已支持任意 `--program-config`，不再硬编码 anchor+sliding。
- 完成严格 paired-run 比较器、模型身份绑定、审计 trace 和 CPU 回归测试。
- 下载并转换官方 Put Back Block 50 episodes（17,612 frames），建立固定 40/10 episode split、任务专属 norm stats 与数据哈希审计。
- 将数据转换、训练和 rollout 入口改成 task-spec 驱动；Cover Blocks 专用旧入口已删除。
- 完成 Put Back Block none 的 50/100/150/200-update held-out 学习曲线，loss 从 `11.258` 降至 `1.043`；完成 stride-1 anchor+sliding context bank 和 200-update full-memory 训练。
- 完成 40-sample full-memory 离线消融：matched loss `0.2855`，empty `0.2910`，matched 明显更低；收益主要来自 sliding，matched 与 mismatched 尚无显著差异。
- 修复 RMBench adapter 的 `cover_blocks` 任务硬编码和带 dropout checkpoint 的 PRNG dtype 恢复问题；Put Back Block 真实 500-step rollout 与 memory 审计链路已跑通。
- 完成同 checkpoint、同 seed 的 anchor+sliding/none 首轮 full-episode smoke：两者均 `0/1`，表明 200 updates 尚未形成可比较的 executor success floor；项目保持 `ACTIVE / executor training`，继续扩大训练预算。
- 对照 released Mem-0 deploy code 后修正 sliding 时间单位：cached actions 期间逐环境步 `observe()`，训练 context 默认 stride=1，validator 要求 memory update 数等于 action step 数。
- 把 DHEM maintenance、causal kinematic event write 与 contextual content+recency retrieve 拆成互不耦合的 typed plugins；三者均完成 unit/combination/smoke tests，并排队做同 checkpoint zero-shot screen。
- 将 RETRIEVE 接口升级为 typed `RetrievalResult`，固定/内容检索都会把候选数、策略及选择证据写入统一 trace；content+recency 的 17,562-latent profiler 冻结 `1e-5` penalty。
- 纳入 2026-08-10 的 HyMeS：proposal 不再声称首个 Agent 修改 embodied memory code，而把贡献收紧为跨任务 typed architecture search、预算匹配普通搜索基线和 locked-task 迁移。
- 纳入 Action-Effect Memory：把“已执行 action 与后续视觉效果的联合表示”登记为独立 learned representation family；它与 observation ring 和 ChainVLA 的未执行 Motion Tail 不重复，但需要 causal action alignment 与独立预训练，因此不混入当前 zero-shot Gate。
- 纳入 OnEvoMemory 并收紧 novelty：它是在固定 short/elite/transition 三-bank 架构中学习 value-guided selection，不是开放 architecture evolution。已把其 `semantic Top-K ∪ Recent-K` 原则拆成预算匹配的 `semantic_recent_union` retrieve plugin；真实 bank 上固定返回 30 项且 `30.19%` 来自最近 30 步之外，paired zero-shot screen 已排队。论文 release 审计已冻结为 `artifacts/2026-08-16-onevomemory-paper-release-audit.json`：正文支持整体架构和 base→offline→online 增益，但没有三库逐项消融；源码中容量、阈值、Top-K、merge/eviction 和 online loss 仅存在于未渲染注释块，所以完整 learned port 等官方 release 或显式冻结 E-MAC 自有选择后再做。
- 审计 NativeMEM 与 TFP 官方实现：NativeMEM 进入 learned encoder/input-token 分支，必须保留 action-supervised tokenizer pretraining；TFP 进入 Gate 1 后的首选 recurrent/utilize 对照，拆成 elapsed-time LTC update、belief state、action-head AdaLN 与 episode-ordered training，禁止封装成单个不可消融开关。
- 将 program 配置迁移纳入可复现性边界：冻结 context 生成时的旧配置，验证新增显式角色字段前后 token/mask 完全等价，并要求 finalizer 在哈希漂移时绑定机器可读 migration audit；full-memory 与 empty-memory 两条训练链均已通过。
- 审计 OptimusVLA 官方 π0.5 overlay：把不重复的 GPM 拆成跨 episode trajectory bank、semantic retrieval、progress alignment 与 flow-prior utilization，登记为 Gate 1 后候选；LCM release 实际使用上一条预测 chunk 且依赖 GPM，与 ChainVLA Motion Tail 重叠，因此不重复加入 catalog。
- 审计 MemoryVLA 官方实现：现有 adjacent-merge 负结果只覆盖 store，完整 PCMB 的 perceptual/cognitive 双流、timestep cross-attention 与 learned gate 是独立 learned family；按官方 ablation 和源码契约登记为 Gate 1 后对照。新增 ECHO hierarchical tree 作为更后层候选；HELM 的 keyframe store 与现有模块重复，SOMA 又依赖当前 Put Back Block 不具备的 movable-view spatial payload，因此均不伪装成当前插件。
- 增加 Agent-facing registry catalog 与任意候选 config smoke：能力清单直接从 executable registry 导出构造参数；candidate smoke 运行真实 build/runtime，自动覆盖 delayed-writer horizon、phase change 和第二 episode reset，并绑定配置哈希与资源计数。
- 补齐 full-memory 三段训练 provenance：finalizer 验证 `0→5599→16800→27999` 的 same-run save/restore 链，并把全部日志归档进最终 checkpoint；不再把单个恢复日志当作完整训练证据。
- 冻结实验 runtime：finalizer 把逐文件哈希的 `memory_harness` snapshot 写入 checkpoint，fixed runner 只从该 snapshot 执行；empty/full 分支复用同一 runtime，successive-run 比较同时锁定 task、architecture 与 executor config identity。
- 审计 BehaviorVLA 与 τ0-VLA 官方 release：BehaviorVLA 归入既有跨 episode action-prior family，只保留其 behavior-prototype 三阶段训练 recipe，避免重复插件；τ0-VLA 的可回退 progress record 是新的 planner lifecycle 候选，但官方尚未发布 high-level 实现，因此只登记接口、不猜测代码。
- 纳入 RoboTTT 的 fast-weight memory family：把 KVB inner update、固定大小 fast-weight state、tanh-gated utilization 与 TBPTT sequence recipe 分开登记；它与现有 token/recurrent memory 不重复，但因无官方 source 且需要联合训练，排在 Gate 1 后。HiMe 被判定为已有 event/key/lifecycle operator 的系统级组合，不重复新增总开关。
- 完成 Put Back Block 1000-update full-memory checkpoint 与同 checkpoint Gate-3：`none / anchor / sliding / anchor+sliding` 在共享 3 seeds 上均为 `0/3` complete success、`max_reward=0`；trace 证明 memory 每步写入、读取并显著改变 action，因此不是插件 no-op。后续源码审计确认这里的 reward 是 final-only，不能再把它称为“阶段 reward”或单独据此证明 motor-skill floor；按分层开发约束仍停止其余 zero-shot 候选，先补齐 control 与可观测 task progress。
- 筛查 Analytic Concept-Centric Memory 与 Cortex：前者的 persistent object / scene graph / action transition / executable skill 是不重复的 structured payload family，登记为具备 RGB-D grounding 与 skill backend 后的 Gate 1+ 候选；后者主要是 planner–executor skill alignment，与现有 key/subtask memory 重复，不新增总开关。两者均不抢占当前 executor control。
- 审计 LoHo-Manip：其 `(completed, remaining)` 记录归入已有可修订 language-progress memory，不复制新 store；2D visual trace 明确划到 memory 搜索空间之外的 planner→executor guidance interface。论文没有隔离文本 memory 的独立收益，项目页也尚未公开代码，因此当前不支付移植成本。
- 筛查 2026-08 新工作并去重：新增 SkillMemo 的 learned skill-routing bank、IOM 的 instance-keyed procedure memory、FM-VLA 的 force-history sensor memory，以及 BridgeVLA++ 中不重复的 canonical 3D spatial rerender；它们都因数据模态、任务 protocol 或联合训练要求进入 Gate 1+，不抢占当前 executor control。Explicit Language Memory 只保留 supervision recipe，LoHo/BridgeVLA++ 已有的语言/temporal 结构不重复登记。
- 继续筛查 action/trajectory/spatial family：新增 CAMP 的 executed-action DCT/VQ recurrent code、TRACE 的 path-signature-routed evidence slots，以及 OpenSPM 的 object-relative 6D pose memory。MemoryWAM、PRISM、WeaveLA、MIRTH 不作为不可消融的论文总开关；其中 WeaveLA 后续只把可独立归因的 completed-segment handoff 拆成 operator，其 learned pooling/action injection 仍作为组合训练层。GeneralVLA-2/ Mem-World 的评测对象不是当前 policy memory，也不进入 registry。
- 完成 executable interface 复核：当前 executor registry 明确限定为 dense-token payload family，异构 payload 候选不塞入 `metadata` 假装可组合；等首个候选真正进入实现时再增加最小 typed contract 与兼容性 preflight。加入两级 store 后 CPU-only 回归测试通过。
- 增加三 control executor-readiness 决策器：必须同时具备 `empty↔full / native↔full / native↔empty` 三份 paired budget comparison，并交叉验证共享 run 指标一致，才能自动选择 Gate-20、full-memory 重训或扩大训练预算；不允许由单份 comparison 或主观视频观察放行 controller。
- 复核 control 的训练预算命名：full checkpoint 是 `none 200 + anchor/sliding 1000 = 1200` optimizer updates，empty/native 各为 `none 1200`；`compare_training_runs` 沿 `initial_weight_params` 递归求和并要求三者均为 `1200×56` optimizer examples 和同一 base params。因此 `u1000` 与 `u1200` 路径名不同但总预算严格匹配，不是漏训 200 updates。
- 审计 MemoAct、VPWEM 与 Chameleon：新增并验证 `tiered_chunk_mean` 两级 store lower bound；VPWEM 只扩展已有 learned consolidator 的递归 summary recipe；Chameleon 拆成 event binding、slow trace、control-index retrieve、fast state 与 Control-JEPA，不把三个完整系统包装成不可消融总开关。全 catalog smoke 会真实越过 chunk migration 和长期区容量边界。
- 审计 MemoryWAM：其 anchor/recent 与已有组件重复，不再新增开关；每个旧 frame 保留 8 个、读取当前帧及因果历史的 learned gist KV，登记为已有 `causal_consolidator` family 的 `per_frame_gist_kv` 训练 recipe。论文两任务平均完整 `92.5%`、w/o gist `40.0%`、full attention `91.5%`；但官方 Apache-2.0 仓库尚未发布训练/推理代码，因此等 Gate 1 与官方 release 后再实现，不用猜测代码污染搜索空间。
- 审计 PAM 公开代码和 RMBench release：把真正不重复的部分拆成 action/context query encoder、range-masked multi-timescale retriever 与 ordered dual-condition utilizer；Put Back 报告 `67%`，但两阶段各 200k-step 且仓库无 license，因此进入 Gate 1 后 learned 强基线，不在当前 checkpoint 上写近似开关。
- 完成 `tiered_chunk_mean` 的真实 latent profiler：50 episodes、17,612 queries 上平均 `12.56`/最多 `14` 个 token 表示平均 `175.68`/最多 `363` 个 source steps，`91.20%` query 超出 sliding-30；与去 anchor 的 sliding 仅 `1.99%` 完全相同。该结果证明模块不重复，但不替代 rollout utility。长期合并 trace 现保留 source range/count；全量 CPU tests `175/175` 与 ruff 通过。
- 审计 CycleManip、MAP-VLA、History-Aware Point Tracking 与 Embodied-SlotSSM：只把不重复的机制拆入 catalog。新增并验证 CycleManip 启发的 `temporal_multiscale` retrieve lower bound；MAP-VLA 的 stage soft prompt/action prior、显式 3D point tracks 与 persistent object slots 都需要新 payload 或训练，保持 Gate 1+，不伪装成免训练开关。
- 完成 `temporal_multiscale` 真实轨迹 profiler：16,012 个 full-history query 均严格返回 30 项，平均 `66.97%` 来自 latest-30 之外，与 sliding 完全相同的 query 为 `0%`；该结果只证明时间覆盖不同，rollout utility 等 fixed module 建立可比较 success/progress 后再测。Agent catalog/smoke、JSON 校验、ruff 与全量 CPU tests `186/186` 均通过。
- 审计 ICML 2026 RoboMME 与官方 MME-VLA π0.5 release：16 个任务覆盖 temporal/spatial/object/procedural memory，14 个模型形成 representation×utilization factorial suite；FrameSamp+Modulator 平均 `44.51%`，无历史 π0.5 为 `17.93%`。新增 `uniform_global` retrieve lower bound，隔离均匀历史覆盖并作为 `temporal_multiscale` 的必要分支消融；完整 multi-patch+AdaLN 路径和 RoboMME locked transfer 留到 RMBench Gate 1 后。
- 完成 RoboMME perceptual patch memory 的官方源码级 factorial 审计：固定 Apache-2.0 policy repo commit `ecf086c3…` 与 HF checkpoint commit `5db4d53…`，核验六个约 11.6–12.3 GB checkpoint 的 SHA-256；CPU smoke 真实执行 uniform 32-frame/512-patch selection、RGB-change TokenDrop 的首帧完整保留/变化 patch 选择、2048→1024 projector、action-memory cross-attention 与 AdaLN。删除过窄的 FrameSamp+Modulator 总开关，改为 `uniform / prefix-causal rgb-change` 两个 retriever与 `context / action-AdaLN / separate-expert` 三个 utilizer，可形成 source-faithful 2×3 typed factorial；它不混入当前 dense-token suite，也不把已有 `uniform_global` 冒充完整方法。加入真实 RMBench selector profile 后，审计 SHA-256 更新为 `d909f6d3961ff162260251d705bcf4b415cc3b3cf95c55698dc0a59bd79fd6ad`，source-candidate config SHA-256 更新为 `05e270dabf2ac6dc5167bcb59644165b1ed752892c0a2f26cdeea9a0dde0d39a`。
- 更新真实轨迹 profiler 同时比较两个 temporal retriever：`uniform_global` 平均 `76.68%` 项来自 latest-30 之外，与 `temporal_multiscale` 的 selected-set Jaccard 仅 `0.1643`，二者都没有退化为 sliding。新插件已通过 Agent catalog/smoke、JSON、ruff 与全量 CPU tests `192/192`。
- 修复跨 path 组合的静默截断风险：`mem0_context` 改为显式逐路径 quota，预算不完整时构建失败，`USE` 只审计实际注入项。增加 `recent_global(15+15)` 组合；16,012 个真实 query 上始终使用 30 项、跨分支无重复，平均 `42.12%` 超出 latest-30，与三种已有时间选择均不完全相同。已有 anchor+sliding/none context bank 到新 schema 的逐 token migration audit 均通过；正在运行的 control 已确认绑定冻结 runtime，不受修改影响。Agent catalog/smoke、JSON、ruff 与全量 CPU tests `198/198` 通过。
- 增加 immutable `candidate_suite` 交付层：自动发现并构建 17 个 architecture，逐个 smoke 后冻结 runtime/config/catalog/summary 与哈希；fixed runner 以单一 `MEMORY_CANDIDATE_SUITE` 接入，不再允许手工拆分覆盖 runtime/config。runner 先依据 checkpoint provenance 做全候选 CPU shape preflight，policy 构造时再与实际 Mem-0 fusion 二次核对；17/17 均兼容当前 `[31,2048]` checkpoint。首个 suite 保存在 `artifacts/2026-08-15-candidate-suite-v1`；全量 CPU tests `206/206`、ruff 与 shell syntax 通过。
- 修复训练 finalizer 的 snapshot 解析：当训练进程指定冻结 config source 时，provenance 校验现在读取最终会附着到 checkpoint 的冻结 program，而不再沿 context manifest 绝对路径读取可变 live config；旧显式角色与新 quota migration 分别按自身 schema 严格校验。正在运行的 empty u1200 冻结 program hash 已与其 audit 逐项复核一致。
- 预注册 readiness 后续预算点：需要增训时以独立 `+1800` stage 把 chain 从 1200 延长到总计 3000 updates，统一绑定 candidate-suite runtime/config，不覆盖旧 checkpoint；full/empty 两条命令已 dry-run。control-only、all-floor 与 full-signal 三个分支的训练顺序和重新比较条件已写入 gate plan，避免看结果后临时选择预算。
- 实现并挂起 `continue_put_back_executor_gate.sh`：readiness 为 full-signal 时自动追加 Gate-17 并组成 Gate-20；control-only 时先延长 full 到 u3000，由 `assess_run_signal` 决定是保留明确 floor artifact，还是继续补齐 native/empty 并重做预算匹配；all-floor 时按 native→full→empty 延长三条 chain 并重跑三 control gate。三个分支 dry-run 均通过，watcher PID `1053516` 等待当前 u1200 决策，不抢占正在运行的 GPU job。
- 补齐 architecture-search 存储成本审计：`USE/WRITE` trace 记录写入前与写入后的逐 path/总 store item 数，run manifest 汇总 peak storage；full-history 500-item bank 不再因同样只注入 30 tokens 而被误判为与 ring-30 等成本。candidate suite 已重新冻结并通过当前 checkpoint preflight；全量 CPU tests `209/209`、ruff 与 shell syntax 通过。
- 发现并接入官方 2026-07 `m1_mix` sanity 路线：该 released Mem-0 executor 无需 planner，官方 Put Back Block 为 `100/100`，比 Cover Blocks planner 链更直接地验证 anchor/sliding。15.3 GB split checkpoint 已按发布方 SHA-256 `df149a...09a` 完整重组，并生成 4.8 GB inference-only 副本。官方 `full` 与同 checkpoint 的 `without_anchor / without_sliding` 推理 intervention 已在 GPU1 队列中；结果会与 π0.5 port 分栏，且不把推理屏蔽冒充论文 Table 2 的精确重放。论文正文与当前 release 均未说明或提供 anchor/sliding 消融的独立训练产物。全量 CPU tests `220/220`、ruff 与 shell syntax 通过。
- 审计 RoboMME-Interference 官方 Apache-2.0 实现，并新增不重复的 `boundary_chunk` retrieve lower bound：相邻 contextual token 分段、chunk max-score、获胜段均匀采样。阈值仅用 40 条 train episode 冻结为 `0.99935`；10 条 validation episode 的 3,207 个 full-history query 均使用 `30/30` slots，平均 `64.90%` 来自 latest-30 之外，仅 `0.405%` 与 sliding 完全相同。该结果只证明 coherent-chunk 结构与预算，不冒充 SigLIP/跨 session 忠实复现，也不声称 policy utility。
- 冻结 `candidate-suite-v2`：18/18 architecture 完成真实 build/smoke，`boundary_chunk` 配置与 train-only calibration 一起进入 config snapshot；全部候选通过当前 Put Back full-memory checkpoint 的 `[31,2048]` preflight。v1 不覆盖，继续作为已有 run 的不可变身份；profiler/runtime 逐索引 parity 已纳入回归，全量 CPU tests `236/236`、ruff 与 shell syntax 通过。
- 继续去重筛查近期方法：SD-VLA 的 static/dynamic token 与 learned recache gate、Keyframe-Chaining 的 learned transition selector/progress query、VQ-Memory 的离散 proprio history 均登记为 Gate 1 learned families；它们需要各自训练或新 payload，不写成当前 checkpoint 上的假开关。
- 补齐 released Mem-0 的 M(n) key/no-key 执行链：同一 Cover Blocks official executor 下，`key` 保留 released history contract，`no_key` 只向 matched no-key planner 暴露阶段边界处最新图；内部历史照常更新以隔离 USE 差异。新增端口所有权检查、server process-group 清理、真实输入图审计和 paired summary；全量 harness CPU tests `240/240`、相关入口 tests `22/22` 与 ruff 通过。该 10-seed 队列随后已完成，最终结果见下方固化条目。
- 补齐 released M1 三条件的逐 episode 审计：evaluator 输出每个通过 feasibility check 的 policy simulator seed，summary 强制 `full / without_anchor / without_sliding` 的完整 seed 序列、官方 Success/Fail 记录和 aggregate success 一致，并输出 `full-only / ablation-only / both` paired transitions；由于 RMBench `setup_demo` 以同一 seed 重置 NumPy/Torch RNG，这也固定了 diffusion policy noise。缺失、重复或错配的 run 不再生成 summary。
- 将相同证据标准扩展到 M(n) key/no-key：no-key summary 不再根据 `seed_start` 假定连续 seeds，而是读取 evaluator 与官方逐 episode log 的真实 seeds/Success/Fail，并同时核对既有 key reference 的 simulator seeds、policy seeds、successful seeds 和 aggregate，输出逐 seed paired transitions。
- 上述 M1/M(n) paired-audit 改动完成后，全量 harness CPU tests `241/241`、相关 Mem-0 policy tests `22/22` 与 ruff 均通过。
- 将 deployable `MemoryStep.metadata` 从 privileged-key blacklist 收紧为显式 safe allowlist，避免 Agent 新增字段时意外读取 task state/reward/success；核心 executable operators 已确认不包含 RMBench 任务名或 success rule。冻结 `candidate-suite-v3`，18/18 architecture 完成 build/smoke，并全部通过真实 Put Back full-memory checkpoint 的 `[31,2048]` provenance/shape preflight；v1/v2 与正在运行的 checkpoint snapshot 均不覆盖。全量 harness CPU tests `242/242`、相关 Mem-0 policy tests `22/22`、ruff 与 v3 revalidation 均通过。
- 完成 empty-mask π0.5 u1200 训练、checkpoint 审计和 3-seed rollout；结果为 `0/3` complete success。它是预算匹配的结构 control，不是 memory 无效证据；原 run 未记录 Put Back task progress，不能再称为已证明的 executor-floor control。
- 修复官方 Mem-0/cμRobo 评测环境：将 venv `ninja` 显式加入 PATH，预编译 CUDA extensions，并为官方 evaluator 增加最大 simulator-seed attempts，防止子进程异常被吞后无限重试。相关 81 项测试、ruff 与 shell syntax 通过。
- 将同一 cuRobo PATH 预检补到 M(n) planner runner；首次 no-key 尝试因该入口漏传 PATH 被 200-seed fail-fast 拦截，失败日志已完整隔离，没有有效 rollout 被废弃。修复后队列从重新审计的 M1 summary 续跑，已生成 no-key seed 100000 的 expert/policy 配对 layout fingerprint。
- released `m1_mix` Put Back 三条件已完成：`full=10/10`、`without_anchor=2/10`、`without_sliding=0/10`；后两者绝对下降 `80/100 pp`，full-only success 分别为 `8/10`、`10/10`。三组 aggregate marker、逐 episode 日志、真实 seed 序列和 checkpoint SHA-256 通过自动审计，复现了 released model 的 anchor/sliding 强执行时依赖。
- 纳入 AgentCanvas/KDLoop：不再声称首个 embodied architecture search，把贡献收紧为同一 π0.5 上的 memory-only typed search、跨任务迁移与 cost Pareto；采用 type preflight、atomic run、axis coverage、post-selection rerun 和 leak rejection，并将 ADAS/AFlow/KDLoop 纳入同预算搜索基线。
- 审计 MemER 论文与新公开 release：它与 Mem-0 key history 共享 planner→subtask→π0.5 边界，但新增 learned recent-frame nomination、重复 vote 保留、1D temporal clustering 和 selected+recent visual utilization，因此登记为 key utility 后最直接的主动写入对照。官方 commit `fb6dbd0e37a8dd76c965ff3f1344f9b448b61911` 提供 SFT/export/deploy/eval 与 Dust checkpoint但无 license；当前只冻结 typed contract 和训练 recipe，不复制源码或提前增加无消费者的抽象。planner-output accuracy 审计新增 stage-vs-executor-pointer 隔离测试，定向测试 `4/4`、全量累计 `244` 项。
- 找到并修复 Cover Blocks π0.5 的 prompt contract 缺陷：Mem-0 native/memory 配置原先没有启用 `prompt_from_task`，repack 也会丢掉 `prompt`，使六阶段数据和 context latent 静默退回 global instruction，而部署 planner 提供 subtask instruction。旧 `1399` memory checkpoint 与缺少训练 provenance、使用同类 global-prompt 配置的 `9999` baseline 均从默认 task spec 移除；task schema 不再接受 checkpoint 字段，clean/fixed runner 强制显式传入经审计 checkpoint。两个 OpenPI 配置现保留逐帧 prompt，context generator 增加 fail-fast 校验与回归测试。Put Back 数据只有一个 task prompt，正在运行的训练顺序与语义不变。
- 重新生成 Cover Blocks M(n) task template：从 79 条可用 episode 中按冻结 seed 选择配置要求的 50 条，并依据逐帧 `task_index` 得到 300 个连续 subtask segment（每条 episode 恰好 6 个、六种真实 prompt 各 50 次），输出 `rmbench_runs/emac_cover_blocks_v2_subtask_prompt/task_template.json`，SHA-256 为 `7e4a611764bc3e2323a54858c28b68430a84fb40e42c942e86537b7e2ebfcb74`。模板选择和 prompt-preservation 均有回归测试；全量 harness CPU tests 更新为 `246/246`，ruff 与 JSON 校验通过。
- 完成 released Cover Blocks executor 的 10-seed matched local planner 对照：key `2/10`、no-key `0/10`，两个成功 seed 均为 key-only；planner 输出 diagnostic exactness 为 `48/48` 对 `25/46`，key 提升 `45.65 pp`。两侧 simulator seeds `100000–100009`、policy seeds `120000–120009`、released executor 与训练 demonstration 数一致。固化 summary SHA-256 为 `6f8a8d451b58dd797711d901aa1d80a8228ae14b08fd17468cdfc17f7997346c`；结果明确标为 matched local reproduction，不冒充未发布的论文 planner ablation weights。
- 增加修正 Cover Blocks π0.5 lineage 的 executor gate：同一 clean runner 只有在显式 `ORACLE_SUBTASK_DIAGNOSTIC=1` 时才启用 simulator subtask prompt，并强制 `diagnostic_spatial` protocol；validator 将 run 标记为 `deployable=false / executor_skill_diagnostic_only`。默认 no-memory baseline 继续拒绝 oracle prompt，防止低层技能诊断混入主结果。
- 将修正 Cover Blocks native u1200 训练、oracle-subtask Gate-3 与 signal decision 封装为 `run_cover_blocks_corrected_executor_gate.sh`；入口固定 template SHA、从 π0.5 base 重新训练且不读取历史 `9999`。有阶段信号时仅放行预算匹配 memory executor 训练，全零时扩大 native 学习曲线，两条分支均禁止 controller；dry-run 契约已进入回归测试。移除 task schema 中 legacy checkpoint 字段后，全量 harness CPU tests、ruff、JSON 与全部 shell syntax 校验通过。
- 修复 Put Back priority queue 的 readiness 缺口：原脚本生成三份 budget-matched comparison 后没有调用 `decide_executor_readiness`，会让 continuation watcher 永久等待。正式队列现保证 decision 在 completion marker 前生成，并加入顺序回归测试；对已经启动的旧进程另设恢复 watcher，只在旧队列退出且 decision 仍缺失时通过临时文件原子补齐。全量 harness CPU tests 更新为 `256/256`。
- 审计 Retrieve-then-Steer：不复制它与 OptimusVLA GPM 重复的跨 episode trajectory store，而只登记 outcome-verified online write、trajectory-consistency filter 与 confidence-adaptive sampler guidance。论文 π0.5 LIBERO-10 为 `92.4→94.4%`，但 unverified memory 和 direct replay 分别降到 `87.6%/87.8%`，因此该 family 后续必须使用 ordered persistent-deployment protocol，不能把 action FIFO 当作复现。
- 收紧训练 provenance：native finalizer 现在要求最终 step、Orbax `_CHECKPOINT_METADATA`、`params/assets` 和 finalized-save 日志同时成立；staged run 还会把 parent checkpoint 与 parent manifest SHA-256 写入 child manifest，比较器逐层复核后才累计预算。已有 active full-memory checkpoint 完成一次显式 parent-link migration，权重未变，migration artifact 记录 old/new hash。真实 full/empty chain 重新验证为同一 π0.5 base、`1200×56=67,200` optimizer examples；native checkpoints `5600/11200/16800` 均已完成异步 commit，metadata SHA-256 分别为 `8ffa04ef6ec41f84264fab5aceda00938aa5664f97e368d62fa2bc73a2b509ca`、`4fa859c859c36cca8562e1563e8818e392048f98b6337875ea0a2472d886ca55` 与 `586494542da8b2ead4cff2c341a444dbdb374973da6208042db708caff6003a6`。新增 readiness 三分支无人值守 dry-run 回归测试后，全量 CPU tests 为 `277/277`，ruff 通过。
- 将 Retrieve-then-Steer 中不重复的 outcome-verified lifecycle 落成 `verified_success_ring` store：episode 内写入隔离缓冲，成功后原子提交，失败时丢弃，未显式上报 outcome 则拒绝 reset；正式 RMBench evaluator 已把真实终局 success/total reward 回传 policy。validator 强制每个 ordered episode 恰有一个 outcome、每条 persistent path 恰有一次 commit/discard，并拒绝读取当前、未来或失败 episode。新增 `verified_success_latent` lower bound，但明确不冒充论文 action-prior steering。冻结 candidate-suite-v4，19/19 architecture build/smoke 完成并通过 Put Back `[31,2048]` checkpoint preflight；artifact 直接记录 4 个成功 items 跨 episode 可检索且失败 episode 未污染。全量 harness CPU tests `271/271`、正式 evaluator tests `38/38`、ruff 通过。
- 新增候选行为去重层：`profile_candidate_distinctness` 在冻结的 Put Back validation split 上比较最终 `memory_tokens + mask` 指纹，而非配置名。14 个可比较 executor、323 个 query 没有 exact duplicate；token-multiset Jaccard `>0.95` 的唯一 review pair 是 `anchor_sliding / sliding = 0.9677`，因其隔离 anchor 的必要嵌套消融而保留，但不计作两个独立创新。`content_recency / sliding = 0.7674`，仍平均替换约四个 history items。robot-state 与跨 episode outcome 候选被显式排除，不能用伪造输入下结论。summary/review SHA-256 分别为 `ba55cee1dbd88da348a893ccdcd1ec528d5c09ab5aa0886875dc11d2d64d8f6a`、`da4a6584782c26a4a26a231b3e1b61a50b08befbfe704132cd6cfde643e31e35`。冻结 candidate-suite-v6，19/19 build/smoke 和真实 Put Back checkpoint preflight 均通过；manifest SHA-256 为 `4b7b582c3b4e3f4faccc36d3fa43e3133148924212f070d015f8e76536c911a1`，v1–v4 不变。
- 增量审计 LifelongVLA 与 Memory for Attention：前者新增 dual-timescale parameter memory + stochastic replay cache，但属于 Gate 2 后 sequential-skill architecture evolution；后者新增 language-conditioned re-perception controller，但当前 RMBench 没有主动视点/perception budget。两者均登记为独立未来 family，不把 LoRA/replay 或 token retrieval 伪装成当前可执行插件。
- 完成 HyMeS 与 HyVLA 官方材料复核：HyMeS 的通用增量是多证据确认后更新 symbolic state，但论文使用的 task program、Qwen3-VL completion prompt、agent prompt、探索预算与修正后 12-task protocol均未发布，故不制造空壳插件；HyVLA compact encoder 有 `90.9/90.1→88.8/88.6` removal ablation 和 Apache-2.0 代码，但与 MEM 的 temporal-spatial short-video family 重复，合并为 learned encoder 实现参照。
- 修复 Put Back u3000 continuation 的 snapshot 漂移风险：移除过期的 candidate-suite-v1 默认值，强制 full/empty 延长训练沿用父 checkpoint 内冻结的 runtime/config，并在启动 GPU 前验证两者身份相同；真实父快照已验证为 runtime `f1f808…`、config `30e319…`。新增 mismatch fail-closed 测试后，全量 CPU tests 为 `278/278`，ruff 与 shell syntax 均通过。
- 补齐修正后 Cover Blocks 的无人值守续跑链：native u1200 oracle-subtask Gate-3 有信号时，从同一 π0.5 base 训练预算匹配的 empty-memory executor，再用同一 checkpoint 比较 matched `key / no_key` planners；若 native 仍为零，先延长到累计 u3000 并复查 executor floor。key/no-key 从共享 Gate-3 自动以不重叠 seeds 补到 `3+17=20` pilot，仅当 pilot success 或阶段指标为正才继续 `+30=50` confirmation。两侧 planner 分别绑定 model index、adapter、base index 和 300-sample training-pair manifest 的 SHA-256；组合比较器拒绝重复 seed/policy-seed/layout evidence，所有分支仍禁止直接放行 controller。
- 审计 Chronos 官方 RMBench release：将其拆成 `observation+proprio state token / selective SSM state update / recurrent-state utilization / full-trajectory credit assignment`，不把 IMLE、二阶 acceleration bridge、AR(2) latent noise 或 temporal action ensemble 计入 memory。论文报告 50 demos/100 trials 平均 `73.6%`，但缺少 RMBench same-head memory ablation，因此登记为 Gate 1 learned recurrent-state 强基线和 source-level reproduction 项，不加入当前免训练 candidate suite。S²-VLA 与 TFP 的 belief-state modulation 重叠，CodeGraphVLP 与 structured concept memory + planner 重叠，均只保留不重复的消融启发，不新增同义插件。
- 审计 VLA-Pro 官方 π0.5 fork：确认其独立 value payload 是 task-specific LoRA，按结构化 procedure state 检索 top-k 并逐 action chunk 融合加载，区别于 LifelongVLA 的 sequential update/anti-forgetting。论文 π0.5 在 RoboTwin/RLBench 为 `40.4→59.3%`、`13.8→20.9%`。release 不含训练权重，且默认 `top_k=0` 固定退回 `turn_switch`，不能直接作为论文配置运行；因此登记为 controller 后的 parameter-memory family，不加入当前 19 项 episode-memory suite。
- 该阶段机器可读的 `configs/source_audited_candidates.json` 登记了 12 个互不重复、明确 `not_executable` 的 typed family；新增完整 MemoryVLA `dual_stream_perceptual_cognitive_memory`，把双 encoder/bank、timestep retrieval、gate、相邻合并、双 action utilizer、reset 与 ordered joint training 分开。官方 commit `d732ea9072bc063399ccc817aed74ab172eb50be`、33.5 GB checkpoint revision/hash/config 和核心 CPU source smoke 已核验；公开配置 `update_fused=false` 与论文 fused-write 文字存在差异，且没有 matched all-memory-off，因此强制官方 checkpoint reproduction 与 π0.5 `none/cognitive/perceptual/dual` factorial 后才可执行。validator 继续拒绝无类型 operator、审计漂移、路径逃逸、重复 family/operator 和伪造 executable alias；该阶段 v2 配置 SHA-256 为 `e1266fbcec63b6ad7abcaae8a7d619e397a13f480c871a984d8533a06fcf9be0`。当前 21 项 fixed candidate suite 和正在运行的 checkpoint snapshot 均未改变。
- 把 WeaveLA 中不重复的 cross-subtask handoff 轴落成 `completed_phase_mean` retriever 与 `fixed_completed_phase_handoff`：episode-long raw ring 按连续 phase 分段，活动段排除，最近已完成段压成 1 个均值 token；同名 phase 重复出现时保持独立。为避免实际 rollout 因无 phase 切换而退化为空 memory，`completed_phase_handoff` 现与 `planner_no_key` 共用 Mem-0 no-key planner，并新增 `key_completed_phase_handoff` 与 key planner 配对；当前两者使用 RMBench oracle prompt-change，只是诊断性边界下界，不冒充可部署 trigger。论文机制审计 SHA-256 为 `cfc73705e01736ecdff2d9dcc49d5380d7ad19b3d255a5019b5b25c4b9aa8bfb`。候选行为去重升级为 v3，直接从训练 manifest 恢复与 latent 对齐的真实 phase sequence，禁止用硬编码单一 phase 把 M(n) handoff 测成空模块。冻结 candidate-suite-v9，21/21 architecture build/smoke 完成并通过 Put Back `[31,2048]` checkpoint preflight；manifest SHA-256 为 `00b7400c12a7e5f663417aa5896725e8085ce1e7871efc112e0714e4b5323bf7`。当前运行实验仍绑定旧 snapshot，不受 v9 影响。
- 增量审计 HALO 官方 Apache-2.0 release（commit `7778638401cc07043e59e2381e84f2383401f491`）：确认其独立机制是 observation/action K/V bank、layer-local learned top-k sparse attention 和 memory-dependent VQA/action co-training，不是当前人工 cosine retriever 的别名。已登记为 `halo_vqa_sparse_attention` 非执行 learned family；因为无训练 checkpoint 且需要新 typed K/V payload，不向 21 个 fixed candidate 中加入无权重占位。源码审计 SHA-256 为 `de0bc565919a7d014480e0d2ff868c10bc7063aba1582c64bb2d7da56548cab8`；typed-edge validator 加固后全量 CPU tests `295/295`、ruff 与全部 shell syntax 通过。
- 补齐 Put Back 自动链的真实断点：u1200 readiness 正向时不再止于 Gate-20，u3000 readiness 也会先补齐同 checkpoint 的 Gate-3/17 固定消融。至少一个 fixed pilot 正向后，才在 candidate-suite-v9、共享新 seeds 和同 checkpoint 下自动运行 10 个非重复 candidate 的 3-episode zero-shot screen；kinematic-event 对 anchor+sliding，其余对 sliding。候选与参照不再硬编码，由 `configs/screens/put_back_fixed_v9_screen3.json` 声明（SHA-256 `901cdf368c7f046ab8585c88cfecf5d6e8fd7d71c36e8ffbf880443fec10cfd3`），typed loader 会拒绝重复候选、同名对照、未知 evidence kind 和 suite 中不存在的 alias。负向或无方向 fixed pilot 会直接保持 candidate/controller gate，正向 zero-shot 也只请求 matched training，不自动放行 controller。两分支 dry-run 与 fail-closed gate 已纳入回归；全量 CPU tests `299/299`、ruff 与 shell syntax 通过。
- 完成 Put Back u1200 三路严格预算匹配 readiness：`full-memory / empty-mask / native-none` 均回溯同一 π0.5 base、每路 `1200×56=67,200` optimizer examples，并使用相同 3 组 simulator/policy seeds；三路均为 `0/3` complete success、`max_reward=0`。源码审计发现该任务 reward 只在完整放回后置 1，不能表示到中心/按按钮的部分进展，因此撤回“零阶段 reward 证明 executor floor”的过强解释。新增只读 Put Back progress trace 与 v2 comparator/readiness/utility contract，按 `0/1/2/3` 记录无进展、到中心、按按钮、放回；success 仍是主指标，progress-only 不放行 controller。审计为 `artifacts/2026-08-16-put-back-sparse-reward-progress-audit.json`，全量 harness tests `306/306`。预注册 u3000 分支继续按 `native → full → empty` 运行，并将使用修正后的 progress gate。
- 增加旧 Put Back action-log 的确定性 progress 回放：逐 episode 强制校验连续 action、原始 evidence SHA-256 和相同 simulator seed，只执行已记录动作、不加载 VLA；回放 success 与原 episode 不一致时拒绝结论。三路必须在 episode/seed/policy-seed/action-count 上严格配对，summary 才报告 full 相对两个 control 的逐 episode 方向，且明确禁止把 3-episode retrospective evidence 当作正式 utility。回放已排在当前 u3000 链之后、Cover Blocks 之前，不抢占训练 GPU；加上 μVLA typed-contract 与 source-candidate payload-family 去重回归后，全量 CPU tests 更新为 `317/317`，ruff、compileall 与新队列 shell syntax 通过。
- 完成 μVLA 官方 MIT release（commit `13e1cf9a34d40726c9f4eeafff464d45c25181bc`）源码审计：其独立最小单元是 recurrent prefix token、每环境步 self-attention write、episode reset、memory/action leakage guard 与 TBPTT credit assignment，不是把 EMA 套在现有 latent 上。论文同 episodic data 的 MIKASA 平均从 `0.48→0.84`，matched-semantics held-out `0.07→0.23`；K=2 的 cue recall 明显优于 K=1/K=8/EMA，open-loop chunk execution 又使 LIBERO Long-10 `95.8→5.4%`。官方 `MemoryModule` 的 CPU smoke 进一步验证 `is_first` gradient routing、continuing-state detach 和 EMA 数值/无梯度 contract。因此登记为第四种非执行 typed family，未来最小 port 从 1-token、K=1/K=2、custom/full mask 和 per-step/chunk-rate cadence 四轴开始；不改变当前 fixed suite 或 controller gate。审计 artifact 为 `artifacts/2026-08-16-muvla-source-audit.json`。

- 完成 Agent 可组合性边界审计：candidate-suite-v9 的 21/21 executor architecture 均由统一七角色 registry/config 构建；planner 侧仍直接绑定 `KeyPlannerMemory`、`Mem0PlannerPolicy` 和 oracle boundary，尚不能宣称开放式 planner-memory search。活动 runner 的 rollout 使用 frozen runtime，但最终 validator 优先 live harness，因此当前不替换 schema v3；已冻结源码哈希、真实缺口、无兼容层的替换内容和触发条件到 `artifacts/2026-08-16-architecture-composability-audit.json`。
- 复核 KEMO v1 与项目页：当前 `causal_kinematic_peak` 的 Cover Blocks 参数 `30/20/8` 对齐论文表值，DINOv2 dedup 阈值为跨任务共享 `0.05`；但在线 peak-window 端点/tie 规则未给出，项目页代码仍为 coming soon。故保留 inspired 标签，不为了“像论文”猜测规则；完整 port 未来按 detector / visual dedup / 16-patch keyframe encoder / cross-attention gate / keyframe-aligned loss 五层实现和消融。
- 增量论文筛查新增两个不重复机制。GMP 官方 MIT source（commit `c23bb405…`）证明 learned gate 是逐 timestep 的 **read controller**：由 held-out all-off/all-on action error 产生标签，冻结 gate 后重训 policy；它不同于 novelty/event writer，也不同于固定 architecture selector，登记为 fixed utility 后首个 controller baseline。源码同时显示 release 尚未在 gate-off 时跳过 attention 计算，论文也说明 Continuous Place Back 常不需要 gate，因此不提前实现无权重开关。`Worth Remembering` 的 V-JEPA-2 rolling distributional surprise + MAD/NMS 则是不同于 current-vs-last cosine 的 **write family**，但只有长期 QA/event-boundary 证据且无官方 code，保留为 Gate 1+ contract。GMP 审计为 `artifacts/2026-08-16-gmp-source-audit.json`。
- 将 controller protocol 拆成两层：Gate 1 后先做 GMP-style 逐 timestep `use_memory` read gate，以 held-out `all-off/all-on` action error 标注、冻结 gate 并预算匹配重训，比较 all-off/all-on/oracle/rate-matched random/learned，并审计 `M(0)` false activation 与真实 token/latency；只有 D1 有增益或成本优势才启动 episode/subtask 级 `none/anchor/sliding/anchor+sliding/key` architecture selector。这样不把“是否读取”与“选择哪套架构”混成一个 controller 结论。
- 增量筛查 AURA-Mem 与 RoboHarness：AURA 的 fast-weight payload 与 RoboTTT 重用，但 action-supervised sparse-write gate 和 target write-rate 是新的 writer/training 轴，登记为 Gate 1 后成本 Pareto 候选；论文 LIBERO-Long 仅支持 `14/60` 成功率持平与 write rate `1.0→0.142`，且代码私有、active 参数不匹配，因此不加入当前 executable suite。RoboHarness 的 linked trajectory、multimodal retrieval、spatial bridge 与已有 trajectory/spatial/handoff family 重叠，只把“异构 policy 入分布交接”保留到 multi-policy architecture evolution，不新增当前 operator。AURA 机制审计为 `artifacts/2026-08-16-aura-paper-mechanism-audit.json`（SHA-256 `09e5e6dfd498fc005434a260164d3a5993e9bc937b37de34d9d485034bc15e2f`）。
- 对正在运行的 Put Back u3000 continuation 做无扰动 handoff 预检：活动进程在旧 v1 readiness 上启动后，progress-aware comparator/decision 更新为 v2；重新从三份原始 u1200 run 生成 v2 comparisons，得到相同 `increase_training_budget_before_more_rollouts` 决策（decision SHA-256 `a9ae95ad499ecae797856cfcf5a4fd66e4f0473605f4a4685ce6ea076ede6bc8`）。full/empty 父 checkpoint 的冻结 runtime/config 身份均为 `f1f808… / 30e319…`，current-script dry-run 确认顺序仍是 `native → full → full Gate-3 → empty → 三路 Gate-3/readiness → 条件式 Gate-20`；活动 bash 的读取偏移已越过 schema check 并恰好停在 full stage 前，因此不重启、不重复 native。相关 comparator/readiness/signal/runner tests `23/23`、ruff 与 shell syntax 通过。
- 完成 OptimusVLA GPM 官方 source + asset 审计：源码固定到 MIT commit `6d0c26b…`，五个 Apache-2.0 发布资产固定到 Hugging Face commit `6995666…` 并逐项核对 LFS SHA-256。真实资产 smoke 验证 6500 条 metadata/packed trajectories 与 2048-D `IndexFlatIP` 一致，self retrieval 得到 score `1.0`、`[10,32]` action prior、NFE `1`，progress `0→1` 令窗口 `0→26` 且 prior 确实变化。默认 progress 只是部署可见的 `executed_steps/max_steps`，不读取 simulator stage；RMBench 仍需比较 elapsed-budget、retrieved-length、shuffled 与 learned observable progress。GPM 已作为第八个非执行 typed family `trajectory_action_prior` 登记，拆成 task embedding、Prior Head training、跨 episode bank、semantic top-k、progress alignment、flow initializer、adaptive NFE 与 episode session；artifact SHA-256 为 `43617a5851691c2dbf71385433f9a5b41a78edf407c4ca15440c7e3b645a457a`，candidate config SHA-256 为 `c26bd1a176b32397cbad34ecf67b9f34c5180841d4ffa9e3ecc57b44537de6f3`。LCM release 缓存上一条完整预测 chunk、只与 GPM 联用且无训练入口，与 Motion Tail 重叠，因此不重复登记。CPU-only 全量 tests `321/321`、ruff、compileall、JSON 与 catalog validator 通过；21 项 executable suite 与活动 checkpoint snapshot 均未改变。
- 用 source-equivalent RoboMME selector 在 50 条 Put Back demonstrations（17,612 frames）上完成真实输出审计：16,012 个 full-history query 中，FrameSamp/TokenDrop frame-set 完全相同率 `0.219%`、Jaccard `0.0981`，证明值得保留为两个 retriever；TokenDrop 平均 `78.76%` patch 位于 latest-32 外。进一步确认 released offline full-episode heap 与 online prefix heap 仅 `83.23%` query 完全一致，`16.77%` offline query 欠预算，平均缺少 `14.16`、最多 `144` 个本应可用的过去 token，虽未返回未来 token。候选已移除泛化的 `rgb_change_patch_budget_retriever` 名称，改成 `prefix_causal_rgb_change_patch_budget_retriever` 并强制 training/inference parity；released 行为只作为 checkpoint compatibility ablation。报告 SHA-256 为 `0048bc0c536b950be4eb5e8881883108dc97d3df2e45aae99e930ad2847d2839`；全量 CPU tests `326/326`、ruff、compileall、JSON 与 source-candidate validator 通过。

- 完成 TRACE v2 与官方 source commit `45bfb5b0…` 审计，将非重复的 `trajectory_addressed_evidence_slots` 拆成真 path-signature address encoder、visual/state evidence encoder、gated slot update、trajectory-conditioned readout、policy adapter、episode reset 和 ordered causal training。论文相同 regression base 的 no-memory/unrouted/full 为 `25.50/52.17/69.23`，支持 routing 独立价值；但 `π0.5=50.47` 只是 comparator，不是 TRACE-on-π0.5。release 无顶层 license/checkpoint；CPU 精确例验证明 `simple` fallback 对不同顺序路径碰撞，deploy runtime 还以无界 deque 每步重算全前缀。因此候选保持非执行，未来 port 强制 signatory fail-closed、bounded streaming address、prefix parity 与 matched ablations。审计 artifact 为 `artifacts/2026-08-16-trace-source-audit.json`；加入第十个非执行 family 后 source-candidate config SHA-256 为 `a132a98beee3434e0415ec01c23f9625535b7aad1254fadb9e843056addefe53`。全量 CPU tests `327/327`、ruff、compileall、JSON 与 catalog validator 通过。
- 完成 AHA-WAM v1 与官方 MIT source commit `471b9815…` 审计，将不重复的双速 memory 拆成 slow video-planner context encoder、6-entry layerwise K/V FIFO、current-observation OVCR、action-DiT utilizer、version/age/stale guard、async refresh/reset 和 phase-offset training。RoboTwin 同 family 的 `Naive-Async / +KV / +OVCR / full` 为 `88.60/91.01/91.47/92.80%`；核心 K/V editor 与 versioned buffer CPU smoke 通过，两个官方 HF repo 的 revision、文件尺寸和 LFS SHA-256 已冻结。关键限制是 memory/context 约 `1.22B` 参数，且 README 指向的 released task checkpoint 使用 `max_action_offset=0` plain config，与论文核心 offset training 有歧义；单线程 runtime 还会静默丢弃 stale request。因此登记为第十一个 non-executable `asynchronous_layerwise_planner_kv_context` family，强制先复现/澄清 checkpoint，再做因果 prefix、统一 stale error 和预算匹配的 π0.5 port；当前 executable suite 与 controller gate 不变。审计 artifact 为 `artifacts/2026-08-16-aha-wam-source-audit.json`，candidate config SHA-256 为 `88010a01a164d497deadd0e5c30e2614a4936a541ae1ae4f8ffb4e939a65bad8`。
- 复核 LaMem-VLA v1、官方项目仓库与 HF release：论文给出了目前较完整的 memory factorial——无/仅 long/仅 short/双 memory 在 SimplerEnv 为 `57.3/65.6/64.6/73.9%`，在 LIBERO-90 为 `92.1/95.4/94.8/97.0%`；native-prefix 也稳定高于 external policy-side 和 raw retrieval。机制上复用已有 dual store、adjacent merge 与 reset，只把 action-hidden writer、multimodal query、dual top-k、query-conditioned condenser 和 source-tagged prefix weave 记为新增 typed axes。GitHub commit `69c0a4a…` 仍只有 README；HF revision `d87f9e3…` 虽上传 30,691,170,718-byte checkpoint（LFS SHA-256 `29da444a…`），但没有代码、配置、环境或 loader，故没有新增 source-audited family，也不下载权重冒充复现。paper/release contract 固化为 `artifacts/2026-08-16-lamem-vla-paper-release-audit.json`。
- 复核 VQ-Memory v2 与官方项目页 commit `d68f5e22…`：它用 50-step/stride-20 joint-state window、256-code VQ-VAE、post-hoc 4-cluster vocabulary 和 40-token memory，作为专用 language IDs 注入 VLM。π0 rule_020 的 none/raw/VQ success 为 `0/0/45%`，20-task 平均 `25.0→56.3%`，说明离散 proprio phase payload 值得加入搜索空间；本仓库已确认 runtime/dataset 都有部署可见的 14D robot-state stream。它与 TFP continuous belief、CAMP executed-action behavior code、AEM vision-action effect 均不重复，已拆成 writer/VQ encoder/cluster mapper/store/prefix utilizer/pretrain/finetune typed contract。但项目页仍将 Code/Dataset/Model 全部标为 Coming Soon，论文没有 VQ-VAE architecture、normalization 或 online boundary 细节，因此不新增 source-audited/executable family，也不猜实现。审计为 `artifacts/2026-08-16-vq-memory-paper-release-audit.json`。
- 去重复核 ReMem-VLA：其 128-query matched factorial 的无/frame/chunk/dual 平均为 `17.75/87.75/84.5/94.5%`，支持在 recurrent state 中搜索更新频率；POP 对 Return Fruit 另有 `34→82%`。但 frame recurrent query、state lifecycle、episode reset 与 ordered training 已被 μVLA typed family 覆盖，因此不登记第二个 family，只保留 `chunk_rate_ema_scheduler` 和独立 POP objective 两个新增轴。论文系统采用 Qwen3-VL-2B、12-layer connector、diffusion head 与 150k-step joint training，截至 2026-08-16 未发现官方 code/checkpoint，不能硬塞到现有 π0.5。
- 深化 HyMeS v1 paper/release 审计：确认其状态为 `plan/stage/binding/count/flag`，PACE 用 proprioception 与最多 5 帧 Qwen3-VL 判断做 `3-of-5` vote；同权重 π0.5/PrediMem/HyMeS 总体 CSR/TSR 为 `52.5/41.3、61.7/45.6、66.2/60.1`，one-shot→rollout-refined program 为 `63.3/53.3→71.8/71.7`。其真正新增轴拆为 symbolic state store、verified transition writer、multimodal verifier、rollout trace refiner 与 frozen-program selector；flow-gradient steering 单列为超出 memory-only 搜索的 utilization。arXiv 源码包 SHA-256 为 `9f058c9d…`，其中 code link 仍是注释模板，未找到官方实现、checkpoint 或修正后的 12-task protocol。故只冻结 `artifacts/2026-08-16-hymes-paper-release-audit.json`，作为后期 task-specific code 强参照，不进入 executable/source-audited suite。
- 完成 KC-VLA 官方 source/checkpoint 深审计：commit `c9991fc7…` 下的 KSM 是 3-frame RGB、hardcoded task id 与 recursive phase 条件的 learned **event writer**；读取端没有动态 top-k，只将 pending/confirmed keyframes 的 latest bounded prefix 连同当前帧送给 policy。46.1 MB KSM checkpoint 已按 `89ffc3cb…` strict-load 并完成 CPU forward；论文 ManiSkill 平均为 no-history/best-stride/KC-VLA `16/57/92%`，其中 context prompt 单独贡献 `88→92`。源码另暴露两个必须修正/消融的边界：pending candidate 在确认前就即时使用，而 threshold-finding 用 ground-truth keyframes 推 phase、与部署递归 phase 不一致。故将其拆成 event encoder、candidate latch、verified writer、multiview-state queue、prefix utilizer、reset 和三阶段 training，登记为第 13 个 non-executable `task_phase_keyframe_history` family；审计 SHA-256 为 `8c35479e…`，candidate config SHA-256 为 `60dd56ea…`。仓库无顶层 license，且必须先做 recursive-prefix calibration 与 official checkpoint reproduction；当前 21 项 executable suite 和活动训练 snapshot 均未改变。全量 CPU tests `333/333`、ruff、compileall、JSON、catalog validator 与 diff check 通过。
- 新论文去重筛查保留 RTCF 的两个实质新增轴，但拒绝重复登记 action-trajectory payload：PMA 为每条成功 trajectory 维护 causal monotonic alignment frontier，用完整在线 visual prefix 联合定位 memory 与 progress；frequency utilizer 只转移 clipped non-DC 低频 motion residual，保留高频与 gripper。matched PI-FAST 的 LIBERO-Long base/frame-NN/time-domain/full 为 `61.6/63.6/50.0/68.6%`，说明 history alignment 与 selective utilization 都有独立价值；四 suite 总体仅 `86.4→88.4%` 且 Goal 轻微下降，需保留 M(0)/负干扰对照。论文没有 code/checkpoint/bank，且漏报 PCA dim、`v_max/γ/F_c/δ_max`，因此只冻结 `artifacts/2026-08-16-rtcf-paper-release-audit.json`（SHA-256 `b6a7bf99…`），将三个新 operator 归入既有 `trajectory_action_prior` family；等 π0.5 产生 same-policy success bank 后再实现，不改变 13 个 source-audited family 或 21 个 executable candidates。全量 CPU tests `334/334`、ruff、compileall、JSON、catalog validator 与 diff check 通过。
- 审计 AtlasVLA v1 并完成空间 memory 去重：它不是 BridgeVLA++ 的固定初始 point-cloud rerender，而是将 wrist RGB 经 streaming depth 与相机/robot-state 标定持续回投，并按 voxel hash、depth confidence、历史衰减、滑动时间窗和首帧 anchor 更新 global latent world state。matched `without world / without ego / full` 在 LIBERO 为 `93.5/95.0/97.6%`、真实长程为 `54.0/56.5/69.5%`，支持保留 world-state path；ego intent bank/相邻合并/cross-attention 与 MemoryVLA 重叠，不重复计 family。论文无官方 code/checkpoint，且未给窗口、decay、confidence calibration、eviction 与 consolidation 细节，因此只冻结 `artifacts/2026-08-16-atlasvla-paper-release-audit.json`（SHA-256 `7fc5d25d…`），登记 `voxel_hashed_spatiotemporal_world_state` paper contract；待可审计 wrist out-of-view task、intrinsics/extrinsics/depth 齐备后实现，不在固定相机 Put Back 上制造假 spatial 插件。全量 CPU tests `335/335`、ruff、compileall、JSON、catalog validator、shell syntax 与 diff check 通过。
- 审计最新 G0.5 v1 与官方 source commit `89f2322…`：其 visual MEM 与 MEM/HyVLA 属于同一 short-video family，不重复登记论文名；新增第 14 个 source-audited payload `bounded_multiview_raw_frame_window`，把 cadence-aware frame writer、bounded raw-frame store、same-patch causal temporal value mixer、current-frame token-drop compressor、native vision-prefix utilizer、episode reset/start clamp 与 whole-history-drop training 分开。官方实现 CPU multiframe smoke 得到 `[8,32]` finite 输出且修改历史会改变当前 token；文档给出 6-frame/5-second/30%-drop recipe和 `1.5×` training slowdown。但论文没有 matched memory-off，活动 task configs 全部 `obs_size=1`，memory checkpoint sidecar 又在 gated HF repo；server 只在 action-chunk recompute 写 history。故候选保持 learned/non-executable，未来强制 policy-query cadence vs per-control-step cadence 消融。source audit SHA-256 `11bb5047…`，14-family config SHA-256 `14aab14d…`；全量 CPU tests `336/336`、ruff、compileall、JSON、catalog validator、shell syntax 与 diff check 通过。
- 完成 OnEvoMemory v1 paper/release 深审计：正文支持 episode-local short/elite/transition 三库、read-before-write、action-conditioned value writer、semantic∪recent read、gated cross-attention 与成功/失败 rollout update，完整系统在 LIBERO-Long-10 为 `86.2→88.6→90.2%`，RMBench SwapBlocks/SwapT 为 `0→10→14% / 0→8→10%`。但没有三库或 writer/read/gate 的逐项消融；更关键的是，`N_s=4 / bank=2048 / semantic-4+recent-2 / threshold / merge / eviction / online loss` 等细节全部只存在于 arXiv 源码未渲染的 TeX 注释块，官方 code/checkpoint 也未发布。因此不新增 payload family或可执行冒牌插件，只保留 value encoder、elite/value-delta writers、三库 lifecycle 与 rollout updater，作为 Gate 1 后 learned-controller 强基线。审计 artifact SHA-256 为 `d479c239b8009e18ab523e13dabd82c8d426b5ab22bd846b21f0b6f7287e4d90`；全量 CPU tests `337/337`、ruff、compileall、JSON、catalog validator 与 shell syntax 通过。
- Put Back u3000 的 native extension 已完成 `50399` 原子提交，manifest 验证父 checkpoint、父 manifest SHA、`1800×56` 新增训练预算与 `memory_enabled=false`。随后 full-memory extension 首次加载 u1200 父权重时暴露真实 continuation bug：Orbax 在 `restore_type=np.ndarray` 下把 NNX dropout 的 typed PRNG key 恢复成 `uint32[2]`，通用 loader 又错误执行 `.astype(key<fry>)`。修复后 weight-only initialization 明确保留新模型初始化的 PRNG state，完整 state resume 路径不变；单元测试与真实 parent checkpoint 均通过。full-memory 首个 `8400` 中间 checkpoint 已于 `07:47:56 UTC` 原子提交并恢复到约 `2.4 it/s`，证明训练和保存路径均继续工作。continuation 新增 fail-closed completed-stage reuse，恢复链会复用已验证 native checkpoint而不重训；失败日志/空 run 目录重命名保留。当前 root/full/watcher PID 为 `3555038 / 3556351 / 3593106`，后续仍按 full→full Gate-3→empty→empty/native Gate-3→readiness→条件式 Gate-20→replay→Cover Blocks 执行。OpenPI loader/train 定向 tests、ruff、format、compileall、JSON、catalog validator 与 shell syntax通过。
- 去重复核公开的 Streaming GRPO memory planner（MIT commit `c17cdd0…`）：其 keyframe buffer、joint nomination 与 selected+recent context 均复用 MemER，不新增第 15 个 payload family；真正值得保留的是 `delayed_outcome_streaming_memory_write_policy_optimization`，用同 seed 的 K 条完整 episode rollout，把最终 progress 的 group-relative advantage赋给早期 nomination。48 项核心 source tests 通过。课程项目自报 ButtonUnmaskSwap SFT/GRPO `26.2/31.4±3.1%`、persistent/reset/no-FIFO `30.0/9.5/30.7%`，但无 checkpoint/raw logs/per-seed records，训练只做 25 steps，且 oracle 执行 press/put-down并提供 phase history。因此该算子只进入 Gate 1 后 learned-controller training reference，不进入 fixed suite或 source-audited payload catalog；审计固化为 `artifacts/2026-08-16-streaming-grpo-memory-source-audit.json`（SHA-256 `94bb02fc01b01ad203bef1428a198aa7b7a9c2dd92bb9702364b6e13c394cc9a`）。加入该训练轴后，全量 CPU tests `339/339`、ruff、compileall、JSON、catalog validator 与 shell syntax 通过。
- 完成 RB-VLA v2 论文/release 去重审计：其 256 维固定 belief payload 与现有 TFP/μVLA/Chronos continuous/recurrent state 重叠，不新增 family；保留真正不同的 `stochastic_action_conditioned_recursive_belief_update`、EMA `t+1/t+5` predictive pretraining 和 inverse-dynamics grounding。论文两物体 P&P 内部消融为 `32.5→57.5→62.5→77.5%`，但不是 same-π0/π0.5 port；训练需要 150M belief、120M diffusion、约 40M timesteps，且无 code/checkpoint/data、loss 常数和部署 sensor contract 不完整，当前 RMBench 也没有已审计 velocity/acceleration/force-torque。因此只冻结 `artifacts/2026-08-16-rbvla-paper-release-audit.json`（SHA-256 `a80a844fd3b5c510ca1865e4aa7ba005496b16d8bb6998de0005faed23c691e7`），Gate 1 前不制造近似 executable 插件。全量 CPU tests `340/340`、ruff、compileall、JSON、catalog validator 与 shell syntax 通过。
- 完成 StemVLA v2 paper/release 去重审计：历史存储仍是 bounded RGB window，不新增第 15 个 family；只保留可替换的 `vggt_implicit_geometry_history_encoder`、`videoformer_temporal_geometry_aggregator` 与 future-geometry distillation。其 matched ablation 在 LIBERO-Long 为 full/去 history/去 future geometry `86.0/83.5/67.0%`，说明 history 有增益但总收益主要来自 prospective branch。标题虽称 Open-Source，实际无 repo/checkpoint/data/source package；论文也未给 history length/cadence/token shape，并在 implicit RGB-only 与 intrinsics/extrinsics/depth/point-cloud 需求之间自相矛盾。因此只冻结 `artifacts/2026-08-16-stemvla-paper-release-audit.json`（SHA-256 `984298f2309261a25846e9bc4296ac4d00a60f041424d1bef42cb93706feb19a`），不制造 executable 近似模块。全量 CPU tests `341/341`、ruff、compileall、JSON、catalog validator 与 shell syntax 通过。
- 完成 HiMe v1 paper/source 去重审计：官方 commit `261766e…` 的图文 multitag CRUD、save/resume 与 FIFO 最小 smoke 通过；论文中 Sentry 为 transient `14→26%`、完整系统 `68→90%`，Query/Create-only 到完整 CRUD 为 `86→90%`，支持后期搜索“何时调用 Planner”和“如何修订 memory”。但 recent window、structured key records 与 revisable progress 均已有对应 payload，故不新增第 15 个 family，只保留 `subtask_completion_sentry_planner_trigger` 和 `planner_generated_multimodal_crud_lifecycle`。source 无 license/tests/executor assets；FIFO 论文/代码为 `8/20`，exact-tag 请求 Top-2 实返 7 条，默认 store 无界，operation policy fail-open，跨 task 共享且 CRUD 无 version/evidence/transaction。故只冻结 `artifacts/2026-08-16-hime-source-audit.json`（SHA-256 `a4b4094666313514c86dc7e3cb197cd17dd0c6a46c56f8121c3c30da6dd92eed`），等 π0.5 key utility 后按本项目的 bounded/versioned/atomic typed contract实现，不复制源码或增加总开关。全量 CPU tests `342/342`、ruff、compileall、752 个 JSON、14-family catalog validator 与 shell syntax 通过。
- 完成 HiMem-WAM v1 paper/source/checkpoint 去重审计：论文在 RMBench 报 π0.5/完整系统 `10.8/26.3%`，Put Back `11/32%`、Cover Blocks `2/19%`，并提出 state+skill+action-chunk event token、boundary-supervised sparse writer 与 teacher-forced warmup；但没有 same-backbone memory-off/Stage-III-off，不能把完整 WAM 增益归给 memory。官方 MIT commit `cca217e…` 的简化 policy 未实现 Qwen 和三阶段 trainer，false gate 实测仍追加零 slot；另一 Wan wrapper 依靠九个 RMBench task rule 写 memory，预测 write logit 只训练/记录不控制写入，12.0 GB LIBERO checkpoint 的文档 eval 又直接实例化 base Wan、绕过 wrapper。故不新增第 15 个 family或 executable 近似，只保留三个 operator/training 轴并冻结 `artifacts/2026-08-16-himem-wam-source-audit.json`（SHA-256 `d1bd73c3922b2a737a002f82eab536becfdc99e9db41a0d7505bdc41bbbfd319`）；完整 joint port 等 π0.5 fixed utility 后再做。全量 CPU tests `343/343`、ruff check、compileall、753 个 JSON、14-family catalog validator 与 shell syntax 通过。
- 完成 WorldScape Policy 2.0 v1 paper/release 去重审计：按 action chunk 保存 VLM perception context 与 4 个自回归 subgoal hidden states 的 `planner_reasoning_trace_event_history`，与 Mem-0 文本 key、KC-VLA RGB/state keyframe、G0.5 raw-frame window 和 AHA-WAM 当前 planner K/V 均不重复；保留 event encoder、gist condenser、global/local/boundary/full view builder 与 event-caption semantic forcing。matched progressive ablation 的平均分为 `40.91→44.67→46.25→47.89%`，支持长期 event memory 的独立 `+1.58 pp`。但官方 GitHub 只有 README、HF 只有 model card，数据与关键容量/mask 均未发布，因此只冻结 `artifacts/2026-08-16-worldscape-policy2-paper-release-audit.json`（SHA-256 `eaf29992aceb5bcc44624439e82aace2b0c9b82ecb6dc70a9f78301df3e60e99`），不加入 source catalog 或 executable suite；14/21 数量保持不变。全量 CPU tests `344/344`、ruff check 与 artifact contract 回归通过。
- 完成 Mem-World v2 paper/release 去重审计：其非重复部分不是另一份 spatial payload，而是用计划 action chunk 经 FK 得到未来 wrist-view query，再按 timestamp/task-relevance surfel 的 visibility×relevance×recency 分数和 temporal NMS 检索历史帧。相同 random-context world model 的 wrist object consistency 为 recent/stride/W-VMem `0.401/0.463/0.502`，支持保留该 query/retrieval 轴；但目标是视频生成，`58→72%` policy 增益来自人工筛选 synthetic trajectories 后 post-training，不是部署 memory。v2 source archive 无 supplement，未发现官方 code/checkpoint/data，top-K/NMS/merge/capacity 等契约也缺失；因此只冻结 `artifacts/2026-08-16-mem-world-paper-release-audit.json`（SHA-256 `e44c197cdcfadd9fa9884d2c767c4634f6f0963b0e92f964dc629edb1dec9a06`），复用 AtlasVLA/BridgeVLA++ spatial family，不增加 source/executable 数量。全量 CPU tests `345/345`、ruff、compileall、755 个 JSON、14-family catalog validator 与 16 个 shell scripts 通过。
- 完成 MEMORA v1 paper/release 去重审计：四类 environment/entity/activity/inferred-knowledge payload 与已有 object/scene/action-transition/skill family 重叠，只保留 evidence-linked entity state-history editor、query-time snapshot rollback、cross-episode regularity consolidation 与 procedure/grounding typed router 四个 lifecycle/retrieve operator。matched EAM-QA episodic/full 为 `60.1/74.5%`，consolidation `+14.4 pp`，entity records 中位压缩 `18.1×`；但 Qwen planning Replay full/episodic 为 `0.338/0.345`，明确说明 consolidation 需要由任务选择而非默认常开。arXiv source SHA-256 为 `22e738…`，项目页当前 404，作者公开 repos 与 GitHub/HF exact search 未发现 code/checkpoint/benchmark；2/2 Unitree 只是 prepared tabletop + fixed controller 定性结果。故冻结 `artifacts/2026-08-16-memora-paper-release-audit.json`（SHA-256 `fefc5c1492f49bf48be882d9fd86e25bdb70da5eaef85a0a33997207e97d6b29`），不增加 14-family source catalog 或 21-candidate executable suite。全量 CPU tests `346/346`、ruff、compileall、756 个 JSON、catalog validator 与 16 个 shell scripts 通过；同时 full-memory u3000 训练已推进至 `10584/50400` microsteps，约累计 `u1578`，loss/grad finite，链保持 ACTIVE。
- 完成 Mimir v1 paper/release 去重审计：world hypothesis tree、ordered task agenda 分别复用 scene/object 与 progress/error payload，真正新增的是 action-bearing interface：observation/action feedback 分路写 world state、postcondition 才推进 task agenda、active goal 只检索 bounded hypotheses、无法形成 evidence-supported object/source/target binding 时 fail closed。Qwen3-VL-8B full/去 world/去 task 的 EB-Habitat SR 为 `65.0/12.5/54.0%`、EB-ALFRED 为 `57.0/52.0/34.0%`，支持 world/task 两侧独立价值；但 dynamic grounding 本身没有被隔离，code/checkpoint/raw runs 未发布，TreeMemory/K/prompt/adapter 与 privileged-state 边界也不可审计。因此只冻结 `artifacts/2026-08-16-mimir-paper-release-audit.json`（SHA-256 `9366ff6fe79029f1f591ae9310b28cecc45b53ebeb787f4029cea9347fe67c44`），作为 MEMORA typed router 的更强 contract，不增加 source/executable 数量。全量 CPU tests `347/347`、ruff、compileall、757 个 JSON、catalog validator 与 16 个 shell scripts 通过；full-memory u3000 同时推进至 `11452/50400` microsteps，约累计 `u1609`，链健康。
- 完成 GESTO v1 paper/release 去重审计：其 4D scene/object backbone 与已有 family 重叠，但 `atomic human-object interaction → complete ordered goal-event partition → persistent object/place evidence links` 是不重复的 `grounded_hierarchical_human_activity_event_memory` payload。matched full/去 hierarchy 的 Text/Time/Space2Event 为 `0.71/0.70/0.73` vs `0.52/0.40/0.53`，full/去 refinement 的 Time/Event2Space 为 `0.70/0.75` vs `0.59/0.61`，支持保留 hierarchy、grounding、unique-context refinement 与 bidirectional relation retrieval。论文只评估回溯 QA，v1 source 仅 11 个 manuscript files，承诺 acceptance 后开源但当前 GitHub/HF 无 code/checkpoint/query/graph release；当前 RMBench 又无 human-activity RGB-D stream。故只冻结 `artifacts/2026-08-16-gesto-paper-release-audit.json`（SHA-256 `71937bee9e3a5a699d4efac1db38b9d39316ab80170e736dde2f1b22a33ccb6e`），不改变 14-family source catalog 或 21-candidate executable suite。
- 完成 Spatial Memory Agent v1 paper/release 去重审计：procedure/lesson payload 与既有 skill/IOM/MEMORA 重叠，只保留 verifier-guided lesson reflection、one-pass write、visit-evidence prior-shrunk TRS、semantic+TRS rerank 与 read-only freeze 五个 operator，作为后期 Agent 自主管理 research/procedure memory 的固定 value baseline。四个 frozen VLM 的五 benchmark macro average 对最强非 SMA 分别 `+2.6/+2.9/+1.7/+2.8 pp`，但 top-3 cards 共用同一 answer reward，不能作 card 因果 credit；主表还从 10 个 calibration pass 选 deployment 最佳 checkpoint，只有单 split。官方项目页 Code Coming Soon，72-file source archive 无 executable，GitHub/HF 无 code/bank/run release。故冻结 `artifacts/2026-08-16-spatial-memory-agent-paper-release-audit.json`（SHA-256 `5cbf4145805c415b9bb8712ab7ad9b84ace3a6e32dfb13fa89a5415662d6a34a`），不新增 payload/source/executable 数量，未来先做 leave-one-card-out/local rerollout 和 pre-registered calibration split。
- 完成 R4DSG v1 paper/release 去重审计：不把 persistent object、scene graph、action transition 与 episodic segment 重复登记，只保留 conservative relative identity association、static/dynamic role、anchor-relative state、persistence-gated anchor-change writer、segment relational writer 与 option-blind retriever。其最强 matched component 证据是 option-blind No-Transition/完整 no-why overall/when `34.9/34.7→37.3/43.1%`，支持 identity+transition 联合价值，但没有拆开两者。官方 MIT repo 完整 21-file tree 只有静态网站和媒体，论文声称 released 的 Day1 scene-graph outputs、代码、Retrieval+ JSON、predictions/checkpoint 均不存在，单 subject、offline QA 也不能外推 π0.5 closed-loop utility。故冻结 `artifacts/2026-08-16-r4dsg-paper-release-audit.json`（SHA-256 `d20824967bde2ba45e188a637607523b35ae504f06213a7fe3047d1490ef7efd`），不改变 14-family source catalog 或 21-candidate executable suite，等待 RGB-only source/output release 与 identity/transition factorial。
- 完成 DreamFly v1 paper/release 去重审计：接受不重复的 `instruction_conditioned_evidence_promoted_visual_landmark_slots` paper-only payload，以及 candidate encoder、cross-observation evidence tracker、persistent/single promotion、anchor/prototype store、read-before-write boundary 和 trained gated utilizer 六个 operator。memory-only progressive SR/SPL `21.55/16.09→24.11/19.85%`，完整/去 memory 为 `31.46/27.17→19.60/18.76%`，支持整个 memory path；但没有逐项消融。论文所谓 causal 仅是 `M_<t` temporal prefix，不是因果推断；memory constructor 固定，而 slot encoder/cross-attention/output/gate 必须随 policy 联合训练。v1 source 9 个 manuscript files、无 project/code/checkpoint/run，且 FIFO、全部 promotion threshold、write utility、prototype update 与 retention/eviction 规则缺失。故冻结 `artifacts/2026-08-16-dreamfly-paper-release-audit.json`（SHA-256 `e5dbe964f81858380b928d47e73b07780b121408947e9c9abf126661ab589f4b`），不硬塞当前 π0.5，也不改变 14/21 catalog 数量。
- 完成 StreamFlow v1 paper/release 去重审计：双时域 payload 与既有 raw-frame/patch/latent/consolidator family 重叠，只保留 pre-ViT I-frame residual selector、reference-anchored adjacent GOP sparse merge、generation-prefix max-frame retrieval、VAS deficit controller 与 post-prefix latent injection。full/去 mid/去 long 在 RTVU 为 `81.55/76.86/80.18%`、VideoMME-Long 为 `62.11/60.33/51.67%`，两条 memory path 均有贡献；matched/shuffled memory correct-answer log-odds `+0.27`。但 VAS timing 对 budget-matched delimiter/random 仅 `+0.23/+0.49 pp`，不能夸大 controller 价值；它依赖 autoregressive attention 和 trained Qwen compressor，teacher labels 的最终 recovery 还看 reference answer。官方无 code/checkpoint/labels/run。故冻结 `artifacts/2026-08-16-streamflow-paper-release-audit.json`（SHA-256 `e8ee0b7e77d74e6de04774cffb68a33072212fba4d8beee12902b93fd1918ead`），不新增 family/executable，不移植到当前 π0.5 gate。
- 完成 DriveVLA-M0 v1 paper/source/checkpoint 去重审计：新增不重复的 paper-only `retrieved_supervision_adaptation_case_memory`，把 cross-episode structural keys、checkpoint-compatible decoder inputs、expert trajectory 与 oracle proposal scores组成一个可检索训练样本；其 utilization 是每场景 fresh static/dynamic LoRA 的三步临时更新，而不是 token 拼接、action prior、预训练 adapter 加载或持续 fast weights。同一 NAVSIMv1 base 的 no-memory/map/map+agent 为 `91.0/91.7/92.3`，offline LoRA/full TTT/LoRA TTT 为 `91.2/92.4/92.3`，说明结构检索和临时适配都有信号。官方 Apache-2.0 commit `0a409e9…` 只发布 Base/Retrieve Model；HF revision `9260a4f…` 只有两个 gated checkpoint，没有 memory bank、TTT/reset、target stats 或 per-seed runs。因此冻结 `artifacts/2026-08-16-drivevla-m0-source-audit.json`（SHA-256 `4bba549de18a137cf2687c1720f0b7795bfb3a7fa0e291a04fd078aa90998c4e`），保留 failure writer、dual-key encoder、labeled store、hierarchical retriever、trigger、adapter reset、TTT utilizer与 score fusion八个 typed operator，不加入 14-family source catalog或21-candidate executable suite，不干扰当前 Put Back u3000 链。
- 完成 Consolidator v1 paper/source/checkpoint 去重审计：它把 occupied routed STM 经 12.35K shared slot-local transform 累积进 LTM，边界后清 KV/STM 而保留 LTM，并让 LTM 直接改变下一段同层 slot routing；因此值得保留的独立机制是 `persistent memory as access state`，不是新的 generic latent payload。公开 30/30 run package 复算得到 frozen-PMNet same-checkpoint learned/identity `87.02/18.32%`，direct routing on/off `87.02/44.38%`，且 immediate STM 两侧均为 `89.90%`。官方 MIT commit `7fe54e5…`、HF revision `ef46f0a…` 和 31 个 checkpoint 完整，但 repo 没有 tests，本地 OpenPI Transformers 版本也不兼容；论文只验证两段 synthetic task并跨 boundary 反传。故冻结 `artifacts/2026-08-16-consolidator-source-audit.json`（SHA-256 `787c6575132adcdbe621efa21711bc69a5eb7d5d0a8c91061811e75cff9f46f5`），保留 consolidation/router/lifecycle/identity/dual-objective 五个 typed contract，不加入 14-family source catalog 或 21-candidate executable suite，不改变正在运行的 Put Back chain。
- 完成 StreamTTT v1 paper/release 去重审计：parallel sliding KV、KVB fast-weight state、near-zero tanh residual 与 ordered training 均复用 RoboTTT，只保留 `input_dependent_momentum_decay_kv_binding_fast_weight_update` 和 `forward_boundary_resumable_large_chunk_ttt_lifecycle` 两个 paper-level operator。same-backbone joint sliding/hybrid 的 RT/ER 为 `68.19/49.58→78.85/59.55%`，但只报告一 seed，缺 wrong/reset/shuffled state intervention；其 matched-memory heuristic baselines 还未做 method-specific retraining。v1 manuscript 自称 work in progress/code will be released，21-file source archive 无 executable，GitHub/HF exact search 也无 official code/checkpoint/data/raw runs，并缺 chunk/head/state-byte/optimizer 等契约。因此冻结 `artifacts/2026-08-16-streamttt-paper-release-audit.json`（SHA-256 `20f37fa2ba7ec61a906b48a7d092c3d689f13e038dfb50c28099318e525adf8f`），不改变 14-family source catalog 或 21-candidate executable suite。
- 完成 Beyond Retrieval/QCR v1 paper/release 去重审计：它不新建 trajectory/procedure store，而是在固定 top-5 与 selected record 后，用 target query+initial observation 生成 workflow invariant、bindings-to-reobtain、applicability 与 verification 四字段 support，是与 IOM source-side distillation 不同的 post-retrieval utilization。2391 targets 上 QCR/Full Trajectory mean success 为 `62.3/51.6%`，online tokens `9.4k/18.4k`；large binding shift stale-binding `10.9/46.9%`。但 targets 从成功 source 受控改写，只测试单条成功 memory，四字段未逐项消融；11-file source archive无 executable，GitHub/HF 无 code/bank/manifest/prompts/raw ledger。因此冻结 `artifacts/2026-08-16-qcr-paper-release-audit.json`（SHA-256 `89cae21aa163755adf68a025e8f0c6aaeb996510087031fa92da5e163683772b`），只保留 target-bound adapter 与 fixed-retrieval intervention 两个 paper-level contract，供后期 architecture-search Agent 使用，不改变 source/executable 数量。
- 完成 EchoVLA v3 paper/release 去重审计：短期 multimodal FIFO 与 dual cross-attention 分别复用 sliding/MemoryVLA，spatial-map payload 也已由 SOMA、concept-centric memory 与 canonical 3D anchor 预留；只保留 `reconstruction_discrepancy_voxel_write_gate`、`coordinate_keyed_ema_voxel_revision_store` 和 `local_frustum_spatial_feature_retriever`。同模型 PnP Counter-to-Stove mobile/static full→w/o scene 为 `17/21→9/16%`，但动态冰箱门 EchoVLA/π0.5 为 `40/50%`，支持把 stale-map ghosting 作为必测负干扰。标题匹配 GitHub commit `afbb447…` 虽在 README 声称完整实现，实际只有 21 个网页模板/静态文件，论文未链接，HF 无模型或数据。因此冻结 `artifacts/2026-08-16-echovla-paper-release-audit.json`（SHA-256 `47a85901c963a65bd55b40b3cfb06e7c0ae305eca30ef973a78c3d6b8e58a296`），不增加 14-family source catalog 或 21-candidate executable suite；待 calibrated deploy-visible RGB-D/pose、out-of-view task 与 split-safe namespace 后再实现。
- 完成 BridgeVLA++ v1 官方 source/checkpoint 审计：temporal anchor/recent-2/sub-goal keyframe 与现有 operator 重叠，仅新增第 15 个 source-audited `viewpoint_aligned_canonical_pointcloud_anchor` family，把 episode 初始点云 writer/frozen store、coarse-waypoint-aligned rerender、language-conditioned encoder、fine-stage corresponding-view cross-attention、reset 与 joint training 拆开。RMBench full/去 spatial/去 temporal/none 为 `96.0/95.4/21.3/18.9%`，Put Back `100/100/38/1%`、Cover Blocks `99/91/5/3%`，明确 temporal 是主增益而 spatial 是遮挡/精定位的定向增益。官方 Apache-2.0 commit `8855333…`、HF revision `e6733c5…`、9.87GB Put Back full checkpoint LFS SHA 与两份 config 已核验；CPU mask/gradient/keyframe-lifecycle smoke 和 compileall 通过。公开树没有 ablation weights，source 将 memory switch 绑定 checkpoint，论文又从头训练 memory block 并联合 finetune，因此只冻结 `artifacts/2026-08-16-bridgevla-plus-source-audit.json`（SHA-256 `24de9b04ce77f994c3483b99a0891a09e9337fafec07ca1c614b94d013078ccb`），不改 21-candidate executable suite；π0.5 port 等 calibrated RGB-D/coarse waypoint 与 matched `full/temporal/spatial/none` training。
- 完成 SERF v1 官方 paper/policy/mapping/checkpoint 审计：动态 environment map 与 AtlasVLA/EchoVLA 已保留的 spatial state 重叠，不重复计 family；只新增第 16 个 source-audited `articulated_robot_environment_relational_neural_point_state`，把 URDF robot surface、object-rigid/FK 双 writer、共享 store、task-instance filter、base 多尺度/双末端/robot-only/environment-only/global 五组 retriever、Point-Transformer encoder、π0.5 prefix utilizer、reset、map pretrain 与 action-LoRA joint finetune 拆开。相同 π0.5 的 image/static/dynamic-env/full 平均 task progress 为 `44.0/54.0/55.4/58.7%`，robot state 在 env-only 上再增 `+3.3 pp`；Task 22 去掉五组 token 分别下降 `3.6/6.1/6.1/1.6/5.0 pp`。MIT policy commit `ea27b7a…`、mapping commit `e68f39a…`、HF revision `1469ac6…`、32.70GB 六组 checkpoint 与 public map assets 已核验；official 8/6-token shape、mask、geometry、gradient smoke 和 compileall 通过。由于使用 prior map/privileged instance labels，且 release 没有 env-only/token-ablation weights、source 也只提供粗粒度 `use_robot_points` 开关，当前只冻结 `artifacts/2026-08-16-serf-source-audit.json`（SHA-256 `5f56bd9e3d989055202eeecaeae09b29ac09a5b9216a3b88146ed1633b210cb6`），不改 21-candidate executable suite或活动 Put Back chain；后续只在 mobile calibrated RGB-D/URDF benchmark 上做 matched 20k-step port。更新后的 16-family catalog SHA-256 为 `27744ecd3aba3be79dd239604ce929347ba2d94a8414bda1ff307809c78d120b`；全量 CPU tests `359/359`、ruff、compileall、769 个 JSON、catalog validator 与 shell syntax 通过。
- 完成 HAM-VLN v1 paper/release 去重审计：bounded recent frame、progress、place/object graph、failure/error 与 planner joint action/write 均被现有 family 覆盖，不新增第 17 个 payload family；只保留 `seeded_one_hop_topological_subgraph_retriever` 与 `abandoned_path_place_failure_evidence_writer`。相同 planner/grounder/controller 的 R2R full 相对最佳 raw history 的 SR/SPL 为 `61.0/48.1` vs `58.3/44.7%`，去 episodic/semantic/reflection 的 SR 分别降到 `53.3/53.3/55.7%`，并相对 full history 少用 `67.2%` episode API tokens。19-file manuscript source SHA-256 为 `c8531a63…`，无 executable/supplement/project/code/checkpoint/trace，且 clustering、fusion、salience、serialization、prompt 与 rejected-stop contract 不完整；方法还依赖 RGB-D/pose、waypoint topology 和 explicit backtrack。故只冻结 `artifacts/2026-08-16-ham-vln-paper-release-audit.json`（SHA-256 `5ebb4fdea7cd05511477bd6b1f603872521e2cb50dd3798f75ac8204d9c6bee0`），作为未来 planner-side graph search 的两个 typed operator，不改 16-family source catalog、21-candidate executable suite或活动 Put Back chain。全量 CPU tests `359/359` 与 770 个 JSON 校验通过。
- 完成 PhysMem v6 官方 paper/source 去重审计：新增第 17 个 source-audited `verified_physical_principle_hypothesis_lifecycle`，其不重复核心是带 trigger/action scope、支持/反证、confidence、verification status 与 evidence link 的跨 episode 物理原则，以及 action-level attribution、targeted experiment、promotion/refutation 和 folding；episode/transition/procedure/semantic retrieval 均复用现有 family。论文 brick insertion full/no-memory/direct retrieval 为 easy `89/83/48%`、medium `76/53/23%`、hard `39/28/8%`，去 resonance/verification/working memory 在 medium 为 `58/64/69%`。官方 commit `bb2b9bb…` 的 200-episode synthetic quickstart 可运行并形成 3 principles，但无论文 checkpoint/data/raw runs/tests/顶层 license；source smoke 进一步证明 bounded writer 配置 2 后实际写到 3、stale principle 不 prune、resume 后五处 store reference 脱节、主动验证无 action constraint 且单次即 VERIFIED 却不足 promotion，默认 passive path 用同一 recent set 拆两半凑 history。故冻结 `artifacts/2026-08-16-physmem-source-audit.json`（SHA-256 `7ebfdce05d6c9bf4373ab6b24591b111786a10a30f47693618f4b9b1c7fc9890`），不增加 executable suite；先修 paper/source parity，并在具有 deploy-visible action-level outcome 和 safe experiment API 的 planner benchmark 上复现，再供 Agent 搜索。
- 完成 VerMem v1 官方 paper/source 去重审计：七种 LTM/STM Add/Update/soft-Delete/Retrieve/Filter/SelectEpisode/Summarize 操作与已有 payload/operator 重叠，不新增第 18 个 family；只保留统一 operation policy、versioned transaction、realized-transition local verifier、terminal-memory global verifier、operation-normalized hierarchical credit 与 `LTM→STM→joint` curriculum 六个 controller/training contract。Qwen2.5-7B 平均 base/no-verifier/full 为 `28.05/41.76/48.01%`，local/global/both 三任务消融均互补。但官方 commit `4782751…` 只定义 schema，没有实际 store/executor/agent loop；source verifier 是 rule heuristic 而非论文 DeepSeek-V3.2，bundled reward loader 的 import 路径失败并把 local/global/credit 全归零，base calibration 还失败四个 ordering checks；仓库无 license/tests/pinned package/checkpoint/data/run。故冻结 `artifacts/2026-08-16-vermem-source-audit.json`（SHA-256 `4ab1b9c1433bdfb82118e3f94398d222811d45c61e9ec17a00075748db7b94d4`），不添加 payload family 或 executable stub；等 fixed π0.5 utility 后，以 harness-native typed transaction 实现 operation-controller 基线。
- 完成 From Signals to Structure v1 paper/release 去重审计：相同 rolling-20 上的 shared board、private scratchpad、10-slot codebook 与 codebook+meta 全部能由现有 shared KV、recursive language summary 和 bounded structured slots 表达，不新增 family/operator。保留三个实验契约：Gate 1 后对入围架构做 capacity×history sensitivity、组合 memory 记录 conflict/staleness/revision、task utility 与 representation structure 分报。论文 cap=25 的 scratchpad/memory-only 为 `0.867/0.747`，cap=64 为 `0.760/0.580`；cap=64 的 memory-only window `5/10/20/40` 为 `0.50/0.34/0.52/0.52`，证明单一预算下排序不可无条件外推。但它只用 gpt-5.4-mini symbolic game，多数条件单 seed，stale/conflict 没有干预验证，官方页无 code/data，GitHub/HF exact search 无 release。因此冻结 `artifacts/2026-08-16-signals-to-structure-paper-release-audit.json`（SHA-256 `ca75b7f2af0c4969bcb3b49bac5e74d5fd3dd7f9d354f151721a0c64f9363463`），只修改 post-Gate 评测协议，不改变当前实验链。
- 完成 Toward a Theory of Hierarchical Memory for Language Agents v1 paper/release 去重审计：不新增 payload、store 总开关或 executable，只保留 `partition π / representative ρ / traversal τ` 独立组合、workload-calibrated self-sufficiency–traversal compatibility、partition coherence/parent-child pruning error 三个接口与评测契约。源码审计确认当前 `AdjacentMergeStore`、`TieredChunkMeanStore` 与 `BoundaryChunkRetriever` 分别融合了这些选择，限制 Agent 自由组装；活动链结束后应以 exact trace replay 迁移并删除 fused 配置。论文只映射 11 个既有系统，明确 matched `ρ/τ` control、SS 校准、dynamic hierarchy 与 data/trace/task alignment 均未验证或仍开放，且无 code/checkpoint/data/run。因此冻结 `artifacts/2026-08-16-hierarchical-memory-theory-audit.json`（SHA-256 `b60c6e7e78ac61b5d77a509c4b1fc729000082827e6716526b75eee349fca386`），当前 17-family catalog 和 21-candidate suite 不变。

- PhysMem 登记后的 17-family catalog SHA-256 为 `442f35f0c5470de84d92eb06a86cfdd122234d48460d9a1ccceaeb8cb71c1dd7`；全量 CPU tests `360/360`、ruff、compileall、771 个 JSON、catalog validator 与 16 个 shell scripts 全部通过，21-candidate executable suite 和活动 Put Back chain 均未改变。
- VerMem 去重落库后全量 CPU tests `361/361`、ruff、compileall、772 个 JSON、17-family catalog validator 与 16 个 shell scripts 全部通过；catalog SHA-256 仍为 `442f35f0…`，21-candidate executable suite 与活动 Put Back chain 均未改变。
- From Signals to Structure 评测契约落库后全量 CPU tests `362/362`、ruff、compileall、773 个 JSON、17-family catalog validator 与 16 个 shell scripts 全部通过；catalog SHA-256 仍为 `442f35f0…`，21-candidate executable suite 与活动 Put Back chain 均未改变。
- 层级记忆设计契约落库后全量 CPU tests `363/363`、ruff、compileall、774 个 JSON、17-family catalog validator 与 16 个 shell scripts 全部通过；catalog SHA-256 仍为 `442f35f0…`，21-candidate executable suite 与活动 Put Back chain 均未改变。
- 完成 ALMA v1 paper/source/release 审计：它是与本项目最终目标最直接的 Agent-memory-search 基线，而不是新的 policy memory family。论文 GPT-5-nano/mini 总体相对 no-memory 为 `+6.2/+12.8 pp`；官方 release 的 215 个 scored candidate 均可编译，但没有 per-episode retrieval trace 或独立 search-seed archive，运行器允许任务专属任意 Python、可写 bind mount 且未禁网，采样器每次重置全局 NumPy seed，并以随机 ID 而非内容标识候选。现已加入 content-addressed、无重复、局部 RNG 的 ALMA-compatible visit-penalized sampler及测试；Gate 1 前保持 inactive，不改 17-family catalog、21-candidate suite 或活动 Put Back chain。完整证据 `artifacts/2026-08-16-alma-source-audit.json` 的 SHA-256 为 `e87eeebe6cf581dadac5cec207710c27eb24fd7c46ba9475269d23a5ec895222`；全量 CPU tests `368/368`、ruff、compileall、775 个 JSON、17-family catalog export/validator 与 16 个 shell scripts 全部通过，catalog SHA-256 仍为 `442f35f0…`。
- 补齐 Gate 1 前的单候选原子边界 `memory_harness.search_candidate`：Research Agent 只能提交三个 JSON 并组合已有 typed operators；preflight 拒绝任意 Python、额外文件、symlink、路径逃逸、非 deployable program 和未知搜索轴，冻结 hypothesis、parent、runtime snapshot、真实 smoke trace 与 path-independent behavior SHA-256。名称或研究文字不参与行为身份，避免靠重命名绕过 archive 去重；所有候选明确标记 `inactive_until_fixed_memory_gate_1`，不接入当前 rollout 链。全量 CPU tests `374/374`、ruff、compileall、775 个 JSON、17-family catalog export/validator 与 16 个 shell scripts 全部通过，source catalog SHA-256 仍为 `442f35f0…`。
- 完成 MemEvolve v1 paper/source 审计并去重落库：其三任务相对 no-memory 为 `+4.24/+5.00/+3.53 pp`，但官方 release 的 Pareto 默认、survivor/task-count/tie-break、parent lineage 与论文不一致；generated code 先改 live tree、再在同进程 import，且无 core tests、paper runs 或独立 search seeds。只保留并实现 `performance_cost_latency_pareto_survivor_selector`，另冻结 cross-task transfer confirmation contract；同一 Pareto pool 强制共用一个声明的 cost metric，不允许混用美元、GPU-hours 和 memory tokens。不新增 policy payload、source family 或 fixed candidate。审计 SHA-256 为 `c92efff1cc7d554ec77470204a42b5d2128ff9b5f7613965915700a1cae8f019`，Gate 1 前保持 inactive。全量 CPU tests `378/378`、ruff、compileall、776 个 JSON、17-family catalog export/validator 与 16 个 shell scripts 全部通过，source catalog SHA-256 仍为 `442f35f0…`。
- 完成 EvoMem v1 paper/release 去重审计并实现搜索侧 writer gate：保留 `lineage_evidence_research_memory_promotion_gate`，以 immutable candidate/run hash 审计 introduction-vs-parent gain、same-parent siblings、机制连续继承的 descendants、downside rate 与 independent recurrence；不把 final score 直接蒸馏成长期经验，也不作单机制归因。论文九类 benchmark 平均 target-metric gain/search speed 为 `6.40%/5.93×`，Circle Packing (32) 五次 full/no-memory mean 为 `2.9338/2.8151`；但公开 arXiv source 只有 manuscript/figures，官方 GigaEvo commit `9b8687e…` 没有 EvoMem runtime、tests 或 run archive，exact thresholds 未发布。故 `memory_harness.research_memory.LineageEvidencePromoter` 强制显式 local policy、fail-closed lineage validation，并保持 Gate 1 前 inactive；不新增 policy payload、17-family catalog 或 21-candidate suite。审计 `artifacts/2026-08-16-evomem-paper-release-audit.json` 的 SHA-256 为 `c67f94c46c21bb740ebde7255b67930d108c8c75a02215037db0d1f68abb34d3`；全量 CPU tests `385/385`、ruff、compileall、777 个 JSON 与 16 个 shell scripts 通过，source catalog SHA-256 仍为 `442f35f0…`。
- 完成 EvolveMem v1 paper/source 去重审计：论文 LoCoMo minimal/strongest/full 为 `0.305/0.432/0.543`，MemBench GPT-4o full 为 `67.9%`，证明 failure-driven config search 值得作为强基线；但官方 commit `db80b6a…` 的三项 claimed new dimensions、LoCoMo prompt flags 和 expected-lift cookbook 均已硬编码，默认 benchmark runner 在第 5 轮直接注入 `evolved_config()`，meta new-dimension 只写 JSONL，默认 elitist 又跳过 meta 参数建议，所谓随机探索实际为确定性 fusion rotation。同一 QA collection 还跨轮承担评分、诊断和选择，release 无 EvolveMem tests 或 paper run artifacts。故只冻结 `artifacts/2026-08-16-evolvemem-source-audit.json`（SHA-256 `02bb9b719e2353ac8cf6af735ea6cb3dc48e54b0515b975dded942327470552f`），作为 Gate 1 后 split-safe structured-config baseline；现有 typed candidate/archive/research-memory/utility-gate 已覆盖全部非重复机制，不新增 policy plugin、source family 或 executable candidate。全量 CPU tests `386/386`、ruff、compileall、778 个 JSON 与 16 个 shell scripts 通过，source catalog SHA-256 仍为 `442f35f0…`。
- 完成 DREAM v1 paper/official-source 审计并落地非重复 spatial lifecycle：整套 DREAM/DynaMem 的 real-robot long-term success 为 `62.5/48.8%`，shared replay memory 为 `0.37–0.63/1.32–10.43 GB`，但论文没有 pose reintegration/RMP 单独消融。官方 MIT commit `6d558e2…` 的 global rebuild 默认关闭，YAML 与 constructor fallback thresholds 不同，regional/local 路径不 reset 全图，point subsampling 也未绑定 seed；仓库可 compile，但没有 tests/checkpoint/paper runs。因 voxel payload 已由 AtlasVLA/EchoVLA 保留，不新增 family；`memory_harness.spatial_reintegration` 的 `SpatialLifecycleProgram` 组合 `PoseGraphReintegrationPlanner` 与 `KeyframeAwareObservationPruner`，输出重建、eviction、feature retention 和 dependent-cache invalidation decision，并要求 calibrated RGB-D archive、optimized pose graph、reintegrator 三项 capability，缺一项即 fail-closed。审计 SHA-256 为 `5e2df56f61a0cf727e22bfbdd9131a7678dd70297cd690f2574974de5dd41556`，模块 SHA-256 为 `9c3ff8e3dedd29342041ed89f22f5e45ef31bbd452041e1bbdec934f4303460a`；当前 RMBench preflight 保持不可选，不扰动活动 Put Back 链。
- 修正 π0.5 三路训练证据语义：u1200 full 的真实 schedule 是 `none 200 → anchor+sliding 1000`，u3000 full 将是 `none 200 → anchor+sliding 2800`；empty/native 则从共同 base 按各自终态条件训练。三路总 optimizer exposure 可比，且同一 full checkpoint 的 `none/anchor/sliding/full` mask 消融仍有效，但该轮不能称为 condition-from-initial confirmation。`compare_training_runs` v3 现冻结 chronological schedule、per-program exposure、precondition updates 与 evidence scope；readiness 跨三份 comparison 核验每个 shared run 的完整 training provenance，并显式输出 `condition_schedule_confirmation=false`。独立审计 `artifacts/2026-08-16-pi05-put-back-training-schedule-audit.json` 绑定四个已完成 checkpoint manifest，只把尚在运行的 full-u3000 记录为 expectation、拒绝冒充完成证据。新增冲突 provenance 与 artifact-scope 回归后，全量 CPU tests `399/399`、ruff、compileall、780 个 JSON、16 个 shell scripts 与 17-family catalog validator 全部通过；source catalog SHA-256 仍为 `442f35f0c5470de84d92eb06a86cfdd122234d48460d9a1ccceaeb8cb71c1dd7`。
- 完成 MMPO v1 paper/release 去重审计：只为 recursive textual summary 保留 `progress+information-gap anchor question`、memory-conditioned response-token entropy、verified-outcome-anchored dense reward 与 future-aware turn advantage 四个 evaluator/training contract，不新增 payload/store。RULER-HQA 56K 的 outcome-only/direct-answer/gap-only/progress+gap accuracy 为 `80.47/78.17/82.02/82.98%`，WebShop MEM1/MMPO 为 `70.87/77.25`；直接奖励答案低熵反而退化，证明 proxy 必须与 unresolved-information probe 和 verified outcome 绑定。论文 source 11 files、无 official code/checkpoint/data/run ledger，方法只验证文本 QA/WebShop，依赖 autoregressive language logits，不能直接评价 π0.5 latent visual/action memory。故冻结 `artifacts/2026-08-16-mmpo-paper-release-audit.json`（SHA-256 `90dee0021120f424071293eaae0bc9115c4abbbdd6e219e39319a09e305e3856`），Gate 1 前 inactive，不改 17-family catalog 或 21-candidate suite。全量 CPU tests `401/401`、ruff、compileall、781 个 JSON、16 个 shell scripts 与 catalog validator 全部通过。
- 完成 BPP v2 paper/project release 去重审计：semantic event keyframe payload 与 KC-VLA 重叠，不新增第 18 个 family；只保留 `binary_semantic_event_rising_edge_writer`、`detector_latency_aligned_keyframe_availability_mask` 与 train/deploy detector parity manifest。四项实机平均 current/naive/PTP/BPP 为 `14.4/12.8/31.8/53.6%`，Mug 的 VLM/oracle keyframe 为 `60/70%`，Drawer 训练时间 naive/BPP 为 `21.67/12.84 h`。但 v2 50-file manuscript 与项目页均无 code/checkpoint/data/run ledger，也未单独消融 latency mask；runtime-only delay 不能覆盖训练 context 或异步 response completion。故冻结 `artifacts/2026-08-16-bpp-paper-release-audit.json`（SHA-256 `f6cc37ed2a4d0988bcddd2f9096e93694d9719525b723ad521d2008e3375df4d`），不改 17-family source catalog、21-candidate suite或活动 Put Back 链。

## 剩余工作

1. 完成正在运行的 Put Back u3000 学习曲线：native `+1800` stage 已完成并验证，当前 full-memory `+1800` 正常训练；随后自动完成 full Gate-3、empty、三路共享 Gate-3 和 readiness。链结束后先回放 u1200 三路旧 action log 恢复准确阶段进度，再启动 Cover Blocks。官方发布模型、同-checkpoint intervention、matched local planner reproduction 与 π0.5 port 必须继续分栏，不得混报。
2. 上述活动链全部终止并通过当前校验后，一次性删除 planner 的 schema-v3 scalar/direct-construction 路径，改为 typed planner-memory、boundary/lifecycle 与 context-utilizer registry；不增加兼容分支，并重新冻结全候选 suite。
3. 只有 u3000 control 建立非零 executor signal 后，才把 `none / anchor / sliding / anchor+sliding` 扩到 20 个共享 seeds；novelty、DHEM、kinematic-event 与 content+recency 继续等待至少一个 fixed module 的 rollout utility，不在全零 checkpoint 上机械消耗评测预算。
4. M(1) anchor/sliding 与 M(n) key/no-key 已分别得到正向 source-level 证据；controller 仍等待 π0.5 上至少一个固定模块通过 utility gate。
5. 让已挂起的修正 Cover Blocks runner 自动完成 native executor floor、预算匹配 empty executor，以及 `key / no_key` 的 `3+17` pilot 与条件式 `+30` confirmation；先比较 planner exactness，再比较闭环 success。key utility 有正向信号后，再实现 MemER 的 learned nomination + vote-cluster visual history。executor floor 建立后，把 `completed_phase_handoff / tiered_chunk_mean / uniform_global / recent_global / temporal_multiscale / boundary_chunk` 加入同 checkpoint、相同 seed、固定 token budget 的低成本 screen；`completed_phase_handoff` 先在 phase-aware validation trace 上做输入指纹去重。只有对应子算子出现增量，才分别训练 WeaveLA 8-query/action-side handoff、MemoAct-style compressor、MME-VLA patch+AdaLN、RoboMME-Interference SigLIP chunk retriever或 CycleManip-style dense pose/progress 模块。VPWEM/Chameleon 等 learned family继续等待 Gate 1。

## 本周验收

- key/no-key 输入协议、checkpoint、seed、global task 与 executor 均可审计；
- paired comparator 不接受 checkpoint、layout、policy RNG 或 prompt 不一致的 runs；
- 全部 harness tests、RMBench 入口 tests、ruff 和 shell syntax 通过；
- proposal 明确标为 `ACTIVE / reproduction in progress`；只有 controller eligibility 等待 utility gate，固定模块复现、executor 训练和诊断持续执行。
